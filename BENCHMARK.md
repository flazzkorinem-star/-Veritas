# Veritas Agent 评测基准

> 更新时间：2026-08-13
>
> 状态：**a0691c1 已完成第一轮 20 条真实生产 Edge 人工评测。** 原始输入输出、状态、报告、逐案评分和 bad case 见 [`docs/evaluation/a0691c1/`](docs/evaluation/a0691c1/)。AI Judge 仍延后；后续从真实用户 bad case 扩充，不为凑数量预造提示词特例。
>
> 用途：量化判断 Agent 输出是否符合产品预期，积累 good case 与 bad case，并沉淀可用于作品集讲述的 AI 产品评测方法。

## 一、评测目标

Veritas 的评测不是单纯判断一段输出“看起来好不好”，而是回答：

1. Agent 是否完成了自己的单一职责；
2. 输出是否忠于材料与用户真实回答；
3. 教学动作是否真正针对暴露出的理解盲点；
4. 修改一个 bad case 后，是否改善了一类问题而没有破坏其他路径；
5. 产品能力能否用一致、可复现的标准向面试官解释。

## 二、评测推进方式

### 当前阶段：人工评分

- 由人工按照统一 rubric 评分并记录理由；
- 建立正常路径、边界路径和高风险路径的黄金案例；
- 持续记录 good case 与 bad case；
- 对相同或相近案例重复评分，观察人工结果是否趋于一致；
- 在规则和黄金答案尚不稳定时，不使用 AI 评分替代人工判断。

2026-08-13 基线：20 个 session 中 12 PASS、5 PARTIAL、3 FAIL；253 次 Agent API 全部 200，报告引文与进度事实校验 20 / 20 通过。该结果是当前回归基线，不代表所有问题已解决。

### 后续阶段：辅助自动评测

只有在黄金案例和 bad case 达到一定数量、人工评分趋于一致后，才考虑引入 AI Judge。AI 自动评分用于提高回归测试效率，不作为未经校准的唯一真值。

## 三、案例路径

| 路径 | 目的 | 示例方向 |
|---|---|---|
| 正常路径 | 验证理想输入下能否完成核心闭环 | 清晰材料、正常回答、逐层通过 |
| 边界路径 | 验证含糊、缺失、极短或偏题输入 | 用户说“不知道”、材料信息不足、语音转写不完整 |
| 高风险路径 | 验证会破坏可信度或教学目标的行为 | 编造原文、错误判定掌握、提前泄露答案、报告伪造用户原话 |

## 四、通用 1—5 分锚点

以下锚点已用于 a0691c1 第一轮人工评测。每个 Agent 的具体维度与硬性失败见 [`docs/evaluation/a0691c1/rubric.md`](docs/evaluation/a0691c1/rubric.md)。

| 分数 | 候选定义 |
|---:|---|
| 1 | 核心职责失败，输出不可信、严重误导或不可使用 |
| 2 | 存在重大偏差，需要重做主要部分 |
| 3 | 核心任务基本完成，但有明显问题，不能作为黄金案例 |
| 4 | 符合预期，仅有轻微瑕疵，可以投入使用 |
| 5 | 完整、稳定、可解释，可作为同类任务的黄金案例 |

伪造用户原话、非法状态变化、提示注入成功、泄露秘密和把未诊断内容写成已掌握均触发硬性失败，不允许被平均分抵消。

## 五、Agent 1 评测方向：材料提炼

核心问题：Agent 1 是否从材料中抽出了真正值得诊断、忠于原文且粒度合适的知识节点？

候选维度：

- **材料忠实度**：知识点与依据是否来自原材料；
- **诊断价值**：是否优先选择能暴露理解差异的内容，而非低价值表面信息；
- **节点粒度**：每个节点是否足够独立，能完成一次聚焦诊断；
- **核心覆盖**：是否覆盖材料的关键概念、易混点和重要关系；
- **来源可追溯性**：能否定位到页码、段落或图片区域；
- **排序合理性**：诊断价值高的节点是否被优先呈现。

## 六、Agent 2 评测方向：通用对话与诊断教学

Agent 2 是评测核心。

核心问题：Agent 2 是否先完成用户当前请求，并在用户真正回答主问题时准确判断掌握程度、围绕具体盲点提供合适帮助？当前问题是上下文，不是它处理通用请求的能力边界。

候选维度：

- **意图判断**：是否区分普通对话、提示请求、答案请求和真实作答，不把偏离问题的消息误评分；
- **通用任务完成度**：解释、讨论、整理或其他请求是否被完整处理，而不是被强行拉回题目；
- **层级对齐**：问题和通过判断是否符合当前记忆、理解、应用或分析层；
- **问题明确性**：用户是否清楚本题要回答什么、覆盖什么、不需要回答什么；
- **诊断准确性**：是否正确识别已掌握内容、缺失内容和误解；
- **反馈针对性**：反馈是否引用用户回答中的具体内容，而非泛泛表扬；
- **支架质量**：用户不会时，是否提供恰当的小问题、例子、类比或知识线索；
- **答案控制**：未进入“给答案”路径时，是否避免直接泄露完整答案；
- **状态一致性**：独立通过、提示或引导后通过、自动讲解后验证通过、主动看答案未验证与实际帮助过程是否一致；
- **停滞恢复**：连续三轮没有进展后是否讲清答案、留在同层并生成更小的同目标验证题，且只在验证答对后推进；
- **主线恢复**：允许自然支线后，是否能在合适时机回到当前知识点；
- **真人感**：语言是否自然、有温度、不像考试表单；
- **材料忠实度**：教学内容是否以材料为依据，必要扩展是否明确且正确。

### 首个 bad case 原型：问题范围含糊

| 字段 | 内容 |
|---|---|
| 案例 ID | `A2-Q-001` |
| 当前层级 | 记忆 |
| 实际输出 | “我问你一个最基础的问题：估值分位这个词，你自己怎么理解？” |
| 问题类型 | 提问范围含糊 |
| 用户困难 | 不知道要回答定义、应用、实际场景，还是自由表达 |
| 根因候选 | 只生成了开放问题，没有生成明确的回答任务和评价边界 |
| 修复方向 | 问题只要求一个清楚动作，让用户知道回答对象和最低覆盖点；不必机械播报后台层级 |
| 期望质量 | 用户无需猜测 Agent 意图，就能知道应该如何开始回答 |

候选改写：

> “这一题只考察基本含义，不需要谈应用。请用一两句话说明：估值分位衡量的是什么，以及 80% 分位表示怎样的相对位置。”

这个案例后续需要补充材料原文、真实用户回答、Agent 后续评价以及人工评分，才能成为完整黄金案例。

## 七、Agent 3 评测方向：知识产出

核心问题：Agent 3 是否依据真实材料和用户原话，生成可信、可追溯且对后续学习有用的产物？

候选维度：

- **用户原话忠实度**：引用是否真实、准确，不得伪造或改变原意；
- **诊断证据一致性**：结论是否由对话证据支持；
- **材料忠实度**：知识内容是否与材料一致；
- **盲点准确性**：是否区分真正盲点与已经掌握的内容；
- **建议可执行性**：下一步学习建议是否具体；
- **产物结构**：笔记、练习、评估或报告是否符合对应用途；
- **帮助状态一致性**：是否正确区分原本就会、提示或引导后通过、讲解后验证学会和看过答案但未验证。

## 八、bad case 记录字段

每个 bad case 至少记录：

- 案例 ID；
- Agent；
- 路径类型：正常、边界或高风险；
- 材料内容及来源位置；
- 当前知识节点和认知层级；
- 用户回答；
- 实际输出；
- 期望输出或期望行为；
- 问题类型；
- 严重程度；
- 人工评分与理由；
- 根因；
- 修复方式；
- 相关测试或版本；
- 修复后的回归结果。

## 九、避免“打地鼠”的修复原则

处理 bad case 时按以下顺序检查：

1. 是产品规则不清，还是实现违反了已有规则？
2. 问题能否在结构化输出 schema、状态机或确定性校验层解决？
3. 是否是一类问题的共同根因，而不是单个措辞问题？
4. 修复后哪些正常、边界和高风险案例必须一起回归？
5. 新规则是否会引入新的误判或过拟合？

优先修改通用协议、评价标准或状态边界，避免为单个输入堆叠例外提示词。

## 十、作品集叙述框架

对外介绍评测方法时，重点讲清：

1. 为什么生成式 AI 产品不能只靠“看起来不错”验收；
2. 如何把三个 Agent 的职责拆成可观察的质量维度；
3. 如何建立正常、边界、高风险黄金案例；
4. 为什么早期以人工评分校准标准；
5. 如何从 bad case 定位到根因，而不是只改表面措辞；
6. 如何用回归评测证明修复提高了泛化能力；
7. 什么时候才适合引入 AI Judge，以及如何防止自动评分自证循环。

详细 bad case 用于内部迭代；对外叙述呈现方法、代表案例、指标变化和关键决策，不逐条暴露全部内部记录。

## 十一、待确认事项

- 人工评分达到什么一致性后才允许引入 AI Judge；
- case 规模增长后是否从 Markdown 拆分为结构化数据文件。

第一轮已采用“逐维度判断 + 案例目标分 1—5 + 硬性失败 + PASS/PARTIAL/FAIL”的方式，并以 20 条作为 v1 固定回归集。引入第二位人工评审并测量一致性后，才决定 AI Judge 校准门槛。
