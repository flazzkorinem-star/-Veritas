# Veritas 产品与 Agent 设计规范

> 版本：1.2
> 状态：已确认，当前产品行为的唯一事实来源
> 更新时间：2026-08-10

## 0. 文档权威与使用方式

- 本文定义 Veritas 当前必须呈现的产品行为、Agent 协议、交互和视觉原则。
- 安全实现与测试底线见 `SECURITY.md`。
- 长期开发规则见 `AGENTS.md`。
- 历史探索、首版实施过程和早期参考统一保存在 `归档文件/`；除非用户明确要求追溯，否则不读取、不引用，也不作为开发依据。
- 如果代码、旧文档和本文冲突，以本文为准；如果本文确实缺少会改变产品结果的信息，立即询问用户，不得自行猜测。

## 1. 产品定义

### 1.1 一句话定义

Veritas 是以通用 AI 为能力底座的材料学习伙伴：它理解用户提供的材料和学习目标，通过自然对话帮助用户讨论、解释、整理、创作、诊断和学习。材料诊断是产品的主要用途，不是对话能力的边界。

### 1.2 用户真正雇用 Veritas 完成的工作

> 当我拿到一份材料却不知道怎样学，或者想确认自己是不是真的懂了，我希望有一个已经读过材料、理解我目标的通用 AI 和我一起处理：该解释时解释，该讨论时讨论，需要检验时再用合适的问题帮我发现并补齐理解缺口。

### 1.3 目标用户

- 想确认自己对材料理解到什么程度的学生；
- 面试、考试、资格认证和复习用户；
- 想查漏补缺，但不想面对机械题库或冷冰冰测验的人。

首版中文界面必须让小学高年级、初中和高中用户不经解释也能理解主要操作。内部可以使用 Bloom、状态机和诊断证据等术语，用户界面不得要求用户先理解这些术语。

### 1.4 核心体验

- 上传材料后直接进入与 Vita 的对话，不设置欢迎页、教学模式选择或目标配置流程；
- 用户是在和一个拥有材料背景与学习目标的通用 AI 对话，不是在填写考试表单；
- 材料是上下文，不是对话围栏；用户可以要求解释、讨论、整理、写作或其他通用任务，Vita 应完整回应，不强行拉回学习流程；
- 讨论、直接讲解、举例、苏格拉底式引导和诊断提问是一组可选方法，Vita 根据当前意图选择，不把任何一种方法固定为默认剧本；
- Vita 每轮只推进一件事，并具体回应用户刚才说了什么；
- 在学习和诊断交流中，用户每轮都应获得明确收益：确认正确之处、发现具体缺口、得到一个例子或支架，或者知道下一步怎样思考；
- 系统诚实展示整份材料还有哪些主题尚未诊断，不用少量抽样冒充完整覆盖。

## 2. 首版范围

### 2.1 必须实现

- 单文件上传：PDF、DOCX、PPTX、Markdown、TXT、PNG、JPG/JPEG、WebP；
- 图片与扫描 PDF OCR；
- 完整知识地图与分批深度诊断；
- 三个真实 Agent 职责，统一调用 `deepseek-v4-flash`；
- 用户进入诊断后，每个有学习价值的诊断主题固定走记忆、理解、应用、分析四层；
- Vita 保留通用问答、讨论、解释、整理和创作能力，普通消息不自动视为题目答案；
- 多主题独立聊天、切换和历史恢复；
- 文字输入、浏览器语音转写、提示、答案；
- 当前主题 0—100 分和材料主题完成进度；
- 任务级诊断报告、Markdown 下载、打印为 PDF、系统分享或复制回退；
- 工作区搜索、重命名、置顶、分享、删除与撤销；
- 桌面和移动端完整主流程；
- IndexedDB 本地持久化；
- 原创卡皮巴拉品牌角色“维塔”；
- 完整错误状态、安全边界、自动化测试、真实浏览器验收和本地生产构建。

### 2.2 明确不做

- 公网部署、自定义域名和线上运维；
- 用户账号、云端历史和跨设备同步；
- 公开任务链接、多人协作和分享原始材料或完整聊天；
- 旧版 `.doc`、`.ppt`；
- 多文件合并为一个任务；
- 用户手动编辑、合并、拆分或重新生成知识节点；
- 层级或节点重做；
- 多个家教角色；
- 笔记、练习和诊断报告以外的知识产物；
- 完整 benchmark、AI Judge、排行榜、徽章系统、积分商城和复杂游戏化；
- 深色模式、国际化、复杂知识图谱和跨节点学习者画像；
- 没有被首版真实功能使用的抽象、配置或兼容层。

## 3. 核心名词

| 名词 | 含义 |
|---|---|
| 学习任务 | 一次单文件上传及其知识地图、所有聊天和报告 |
| 材料模块 | 按原文件结构和语义划分的章节或内容区块 |
| 知识条目 | 从材料中抽出的原子内容，分为核心与辅助 |
| 诊断主题 / 节点 | 由若干高度相关的核心条目组成，拥有一段独立聊天和四层状态 |
| 层级 | 后台固定顺序：记忆、理解、应用、分析 |
| 材料进度 | 已完成诊断主题数 / 总诊断主题数，不是跨主题平均分 |
| 当前主题得分 | 当前主题四层累计 0—100 分，只用于直观反馈 |

代码中统一使用 `Task`、`MaterialModule`、`KnowledgeItem`、`DiagnosticNode`、`Stage`、`Message`、`Report`。用户文案优先使用“材料”“主题”“当前得分”，避免“节点”“认知层级”“诊断证据”等术语。

## 4. 完整使用流程

```text
打开主界面
  → 在聊天输入区上传一份材料
  → 自动创建任务
  → 读取与解析材料
  → 建立完整知识地图并聚合诊断主题
  → 自动打开第一个主题
  → Vita 判断材料质量与学习价值，并提出第一道有意义的问题
  → 用户用文字或语音回答、表达目标、提问、讨论或下达其他任务
  → Vita 完成用户当前请求；用户确实在回答主问题时，后台按四层逐步检验与教学
  → 当前主题完成，材料进度与得分直接更新
  → 用户暂停、切换主题或继续下一个主题
  → 每完成一个主题自动更新任务级诊断报告
  → 全部主题完成后任务完成
```

### 4.1 上传前

- 页面直接显示完整主界面；
- 没有选中任务时，聊天输入区不可发送普通消息；
- 占位文案用简单语言提示先上传材料；
- 上传按钮是主要操作，位于聊天输入区附近；本地历史仍在恢复时也允许选择新材料，只有已经开始导入后才暂时禁用。

### 4.2 上传、解析与知识地图生成

- 选择文件后立即创建任务并保存原始 Blob；
- 能测量时显示真实字节、页数、幻灯片数或 OCR 页数进度；
- 模型处理无法精确测量时显示当前阶段、不定进度和已用时间，不伪造百分比；分块提取只按已经完成的分块数更新计数，不能把已发出的请求算作完成；
- 处理阶段至少区分：读取文件、解析内容、识别图片、整理知识地图、准备第一个主题；
- 整份材料从读取到首次对话最多处理 5 分钟，其中知识提取、覆盖审计和首问生成合计最多 3 分钟；达到上界立即停止剩余工作，保留原文件并允许重试；
- 分块知识提取固定最多并行 2 个请求，结果仍按原文顺序进入覆盖审计；所有单次超时和重试都服从整份材料的剩余总预算；
- 覆盖审计发出前校验实际请求字节数，超过同源 Agent API 上限时提前失败，不等待服务器返回 413；
- 处理失败时说明失败发生在哪个阶段；用户取消后立即停止当前任务，不继续等待后续模型步骤；
- 失败任务保留在工作区，显示可理解的原因，并提供重试与删除。

### 4.3 生成完成

- 用户不确认、不编辑知识地图和诊断主题；
- 系统自动打开排序第一的主题并开始对话；首次回复简短判断材料质量与学习价值，并提出一道服务于后续理解的主问题，不批量出题；
- 材料重点混乱或不适合直接记忆时，Vita 应先指出问题并把主问题放在真正值得理解的概念、机制或关系上，不能为了维持流程考查低价值信息；
- 不弹出学习目标、学习模式、用途选择或节点确认步骤。

### 4.4 当前主题完成

- 不弹出完成卡片、分层统计、徽章或额外总结面板；
- 节点状态、材料进度和当前得分原地更新；
- 对话中只保留家教针对最后一次回答应有的自然反馈；
- 桌面用户可从主题列表切换，移动端提供低干扰的“下一个主题”导航；
- 已完成聊天可回看但不可重做四层和改分。

### 4.5 恢复

- 再次打开同一浏览器时恢复最近任务、当前主题、当前后台层级、主问题、提示状态、无进展计数、消息、输入草稿和报告；
- 页面关闭或刷新导致处理中断时，不把任务继续伪装成“处理中”；保留原始文件并明确显示“处理已中断”，允许用户重新处理；
- 切换主题再返回时恢复原问题，不重新生成，不丢失提示和得分。

## 5. Agent 1：完整知识地图

### 5.1 职责

Agent 1 先覆盖材料，再判断哪些内容值得理解、哪些只需参考，以及哪些适合进入诊断。它不能只选择少量“最重要节点”后静默丢弃其他重要内容，也不能把材料中出现的每个字符串都当成学习目标。

### 5.2 两阶段流程

1. **分块提取**：Flash 非思考模式依据原文件页、幻灯片、段落或图片区域提取模块、知识条目、来源和潜在误区；
2. **全局整理与覆盖审计**：Flash 低强度思考模式合并同义项、去重、识别跨段关系、聚合诊断主题并核对所有来源与条目的归属。

所有输出经 Zod 校验；发现孤立核心条目、无来源条目、重复归属或过大主题时，只重试受影响阶段。

### 5.3 数据契约

```text
MaterialModule
  id
  title
  sourceRange

KnowledgeItem
  id
  moduleId
  title
  summary
  kind: CORE | SUPPORTING
  diagnosticRationale
  sourceReferences[]
  commonMisconceptions[]

DiagnosticNode
  id
  moduleId
  title
  objective
  knowledgeItemIds[]
  sourceReferences[]
  canonicalUnderstanding
  commonMisconceptions[]
  bloomTargets.memory
  bloomTargets.understanding
  bloomTargets.application
  bloomTargets.analysis
  order

CoverageAssignment
  knowledgeItemId
  disposition: DIAGNOSED_IN_NODE | SUPPORTING_IN_NODE | REFERENCE_ONLY
  nodeId?
  reason?
```

### 5.4 聚合规则

- 不设整份材料总节点硬上限；
- 一个主题通常聚合 2—5 个高度相关、可以共享一套四层目标的核心条目；
- 独立概念不能为了减少数量被强行合并；
- 案例、数据和延伸说明进入知识地图并绑定主题，不为每一条单独走四层；
- 可随时查询的代码、编号、名单和孤立数字默认只作参考；除非用户目标明确要求记忆，否则不能因为它们容易出题就升级为核心条目；
- 核心条目优先承载概念、机制、因果、边界、区别和可迁移方法；四层目标必须检验理解价值，不能只是复述或 OCR 记忆；
- 每个核心条目只能属于一个主要诊断主题；
- 每个辅助条目必须绑定主题，或以 `REFERENCE_ONLY` 记录不单独诊断的理由；
- 节点排序优先考虑材料核心程度、易混淆程度、迁移价值、前置性和来源充分性；
- UI 按材料模块折叠展示全部诊断主题，不能把未显示等同于已覆盖。

### 5.5 学习目标

- 学习目标是持续更新的对话上下文，不是进入产品前必须填写的配置；
- Agent 根据材料建立覆盖核心概念、易混点、应用方法和边界的默认目标；
- 不显示目标卡片、固定用途选项或“调整目标”；
- 用户在聊天中自然表达面试、考试或其他用途时，Vita 记住这一目标，并调整后续解释、例子、主题顺序和问题场景；
- 目标不明确但会实质影响帮助方式时，可以自然追问一次；不影响当前回答时先完成用户请求，不把对话变成需求访谈；
- 用途变化不删除知识条目、不缩小材料覆盖、不改变已完成结果。

## 6. Agent 2：Vita 通用对话与学习

### 6.1 身份

Agent 2 是唯一直接与用户对话的 Vita。它以通用 AI 的能力理解和完成用户请求，同时掌握当前材料、学习目标、主题和诊断状态。可靠、耐心、有温度是交流品质，不是固定人格表演；它不是考官、客服，也不是只会说“很好，请继续”的问答机器。

Vita 可以直接回答、讨论、解释、比较、整理和创作。它可以指出材料质量差、重点不清或某项内容不值得记，也可以在用户不想学习时正常完成其他请求。学习方法是内置工具箱，不是对用户施加的固定流程。

### 6.2 后台固定状态机

```text
ResponseMode = CONVERSATION | START_DIAGNOSTIC | EVALUATE_DIAGNOSTIC

TaskStatus  = PROCESSING | READY | IN_PROGRESS | COMPLETED | FAILED
NodeStatus  = NOT_STARTED | IN_PROGRESS | COMPLETED
StageStatus = LOCKED | ACTIVE | PASSED | PASSED_WITH_HINT | PASSED_WITH_ANSWER

MEMORY → UNDERSTANDING → APPLICATION → ANALYSIS
```

- 上传完成后默认建立第一道主问题；用户确实在回答已经显示的主问题时，才评价回答并推进诊断；
- 用户提问、讨论、下达任务、换话题或暂停时，Vita 完整回应当前意图，保留主问题、提示入口、答案入口和诊断进度，但不把这条消息评分；
- 用户在文字中表达请求提示或直接看答案时，模型按语义调用与按钮相同的提示或答案流程，不能依赖固定关键词；
- 每个主题固定按四层前进，不跳层、不回退；
- 每层只生成一个主问题；
- 未通过时围绕同一考察目标追问，不能偷偷换题；
- 四层主要是后台机制，聊天和右侧主视图默认不播报层级名称；
- 如果必须向用户解释，使用“基本含义、自己的解释、实际使用、拆解关系”等普通语言。

### 6.3 四层通过证据

| 后台层级 | 通过标准 |
|---|---|
| 记忆 | 能识别概念或说出基本定义 |
| 理解 | 能用自己的话解释，而不是仅复述原文 |
| 应用 | 能把概念放进具体场景并说明怎样使用 |
| 分析 | 能拆出机制、因果、边界或对比关系 |

### 6.4 诊断问题契约

- 问题只要求用户完成一个明确动作；
- 用户应知道需要回答基本含义、解释、场景还是关系，但不需要看到层级术语；
- 问题自然包含必要边界、关键覆盖点和适合的回答长度；
- 禁止只问“你怎么理解”“你怎么看”“说说你的想法”；
- 禁止一次堆叠多个独立问题；
- 不为了显得严谨而写成评分 rubric 或长清单。

### 6.5 首次判断与首问

- 不硬编码固定开场白；
- 首次回复简短说明材料的质量、重点或可能的学习价值，并提出一道有意义、动作单一的首个主问题；
- 首个问题必须服务于材料理解和默认学习目标，不能考查仅因容易提取而出现的低价值文字；缺少关键目标时可以把目标自然融入对话，但不能先弹配置流程；
- 默认不宣读主题数量、系统处理过程、教学模式、学习目标和可调整选项；
- 表达应直接、具体并贴合材料，避免机械宣读处理流程或固定复用规格示例句；开场质量必须通过真实材料多次调用和人工体验调整。

### 6.6 回答分类

回答分类只在 `EVALUATE_DIAGNOSTIC` 中使用。不能为了复用分类器，把普通聊天、用户提问或通用任务标成 `OFF_TOPIC`。

```text
CORRECT | PARTIAL | INCORRECT | TOO_SHORT |
COPIED | MISCONCEPTION | OFF_TOPIC | NO_ANSWER
```

只有 `CORRECT` 通过当前层，其他分类选择不同教学动作：

- `PARTIAL`：承认具体正确部分，只追问缺失点；
- `INCORRECT`：指出冲突或缩小问题；
- `TOO_SHORT`：明确少了哪一部分；
- `COPIED`：要求换成自己的话，并提供新的表达角度；
- `MISCONCEPTION`：使用反例、冲突场景或类比帮助修正；
- `OFF_TOPIC`：自然回应一两句，再桥接回原问题；
- `NO_ANSWER`：先说明究竟要回答什么，再提供能开始思考的具体案例或线索。

### 6.7 每轮最低学习收益

在学习或诊断对话中，每条 Vita 回复至少完成一项：

- 点出用户回答中一个具体正确之处；
- 说清一个具体缺失或误解；
- 提供一个新例子、类比、反例或思考支架；
- 把下一步缩小成用户容易接住的明确动作。

禁止空泛表扬后继续出题，禁止重复同一种鼓励句式，禁止与用户原话无关的模板评价。

### 6.8 追问与无进展

- 模型返回 `progress = ADVANCING | STALLED`；
- 只要用户产生新的正确理解，连续无进展计数归零；
- 三轮是连续 `STALLED` 的阈值，不是总追问上限；
- 用户持续推进时允许超过三轮；
- 连续三轮无进展后直接讲清完整答案，当前层进入 `PASSED_WITH_ANSWER` 并继续下一层；
- 闲聊不计入无进展；用户明确想暂停时停止追问。

### 6.9 提示与答案

#### 提示

- 只有用户点击“提示”才设置 `hasRequestedHint = true`；
- 提示可多次使用，力度依次为方向、案例或类比、接近答案的结构化线索；
- 模型返回的 `hintLevel` 必须与当前点击请求一致；不一致视为该提示操作的无效输出，只重试当前提示；
- 第三次后不再继续泄露更多，用户可以继续尝试或使用答案；
- 提示后答对记为 `PASSED_WITH_HINT`，仍得 25 分。

#### Agent 主动支架

- 用户说不知道、问题不清楚或明显卡住时，家教必须主动澄清、举例或搭支架；
- 主动支架不改变用户可见状态和得分；
- 内部以 `scaffoldEvents` 记录类型、原因和回合，供报告诚实描述学习过程；
- 主动支架不能把完整答案伪装成线索。

#### 答案

- 用户点击“答案”时，家教提供当前问题的完整、可理解答案；
- 当前层进入 `PASSED_WITH_ANSWER`、得 0 分并进入下一层；
- 产品不鼓励优先看答案，但不得羞辱、警告或惩罚用户。

### 6.10 模型与代码边界

模型负责：理解用户当前意图、完成通用对话任务、判断是否开始或继续诊断、提炼学习目标，以及分类诊断回答、提取正确证据和缺口、识别误解与进展、生成问题、反馈、支架、提示、答案和自然过渡。

代码负责：只在诊断模式中执行四层顺序、唯一主问题、提示点击、无进展计数、三种状态、25/0 计分、节点和任务完成；同时负责持久化、切换恢复，以及拒绝模型提出的非法状态变化。模型返回通用对话时，代码必须保持诊断状态原样。

用户消息先返回严格结构化的模式决策。`CONVERSATION` 只包含自然回复和可选的学习目标更新；`REQUEST_HINT` 与 `REVEAL_ANSWER` 复用现有提示和答案流程；`START_DIAGNOSTIC` 只为没有主问题的恢复场景保留自然过渡和首个问题；`EVALUATE_DIAGNOSTIC` 才包含：

```text
classification
isCorrect
progress
correctEvidence[]
missingPoints[]
misconceptions[]
teachingMove
assistantMessage
```

自由文本不得直接修改分数和状态。

模型为当前模式附带 `question: null`、`scaffold: null` 等已知无害空字段时，代码只按显式白名单剥离；分数、阶段、完成、非法枚举、非法引用和与当前诊断状态冲突的模式不得因此被忽略或降级。状态冲突只在当前操作的总尝试预算内反馈脱敏字段路径并定向修复；修复仍失败时返回受控错误，不展示无效回复、不改变状态。

### 6.11 节点切换与主线恢复

- 每个主题独立保存消息、输入草稿、当前层、主问题、提示状态和无进展计数；
- 返回主题时恢复原问题，不重新生成；
- 已完成主题仍可继续普通对话，但不能重做四层、改分或重新完成；
- 不显示“继续上次的理解层问题”等内部术语，可用轻量普通语言提示“接着刚才的问题”；
- 节点状态互不影响，不用一个主题的表现自动替另一个主题通过。

## 7. 计分与反馈

### 7.1 当前主题 100 分

| 层级结果 | 得分 | 内部状态 |
|---|---:|---|
| 回答通过 | 25 | `PASSED` |
| 点击提示后通过 | 25 | `PASSED_WITH_HINT` |
| 点击答案或自动给出完整答案 | 0 | `PASSED_WITH_ANSWER` |

- 每个主题独立 0—100 分；
- 分数是文化上容易理解的即时反馈，不宣称是科学测量、智力判断或严格掌握度；
- 主题可以在 0 分时完成，因为“是否走完”和“得分”是两个独立状态；
- 不允许重做和改分；
- 不计算任务级平均分。

### 7.2 主界面只显示两组信息

1. **材料整体**：已完成主题数 / 总主题数和进度条；
2. **当前主题**：主题名称、进行中或已完成状态、当前 0—100 分。

主诊断区不展示学习目标选择器、“学习进度 / 掌握证据”双指标、默认展开的四层列表、节点完成总结卡、徽章或分层统计。

## 8. Agent 3：任务级诊断报告

### 8.1 生成方式

- 首版只实现诊断报告；
- 每完成一个主题，后台自动更新任务报告；
- 完成第一个主题后报告入口可用；
- 所有主题完成后生成最终版并标记任务完成；
- 报告保存在 IndexedDB。

### 8.2 固定内容

1. 材料名称与诊断时间；
2. 完成主题数与材料总体进度；
3. 已诊断、尚未诊断、辅助覆盖和仅作参考内容；
4. 各主题得分与三种层级状态；
5. 用户已经理解的内容；
6. 主要理解盲点与误解；
7. 用户原话证据；
8. 家教主动提供过的支架；
9. 本次学会或修正的内容；
10. 下一步建议与材料来源引用。

报告不得伪造用户原话，不得把 Agent 的答案写成用户自己掌握的证据，也不得把尚未诊断内容描述为已学会。

Agent 3 接收已完成主题中按顺序排列的用户与 Vita 完整对话、学习目标和确定性状态，结合前后文判断哪些用户消息真正体现理解；不得用固定关键词过滤普通对话或操作请求。代码只验证报告引用的消息真实存在且角色为用户，不替模型做语义判断。

报告的用户可见文案必须使用自然中文，不显示 `PASSED`、`PASSED_WITH_HINT`、`PASSED_WITH_ANSWER` 等内部枚举，也不解释“确定性分数”等实现术语。

### 8.3 查看、下载与分享

- 网页内查看；
- 下载 Markdown；
- 打印或保存为 PDF；
- 支持 Web Share API 时分享报告文本或 Markdown 文件；
- 不支持时复制报告摘要；
- 不分享原始材料、完整聊天和公开任务链接。

## 9. 信息架构

### 9.1 桌面端：语义三大区域，视觉两区

```text
任务工作区 | 当前主题聊天
             ↳ 主题与学习进度按需展开
```

语义上：

1. 左侧工作区；
2. 中间核心对话区，承载当前主题聊天；
3. 主题列表与诊断信息仍保持独立语义，但只在用户主动打开时以浮层或抽屉出现。

建议初始宽度：工作区约 260px，对话区占据其余空间，对话正文保持舒适行宽。桌面端不得让空主题栏或低信息量诊断栏长期占据首屏；比例必须以真实浏览器中的阅读、点击和留白效果为准。

### 9.2 工作区

- 新建任务或上传材料；
- 历史任务列表；
- 搜索任务标题和材料文件名；
- 收起为紧凑栏；
- 三点菜单：重命名、置顶、分享、删除；
- 三点菜单必须脱离任务列表的滚动裁剪，在长列表底部也完整留在视口内；按 Esc 或点击菜单外关闭并把焦点还给触发按钮；
- 重命名与删除确认使用真正的模态对话框：打开后焦点留在对话框内，Esc 可关闭，关闭后焦点回到原触发按钮；
- 置顶任务内部按最近更新时间排序；
- 删除前确认，删除后提供短时撤销；不提供批量删除。

### 9.3 主题列表

- 桌面端由对话顶部的“主题”入口按需展开；移动端继续使用抽屉；
- 按材料模块分组折叠；
- 每个主题显示未开始、进行中或已完成；
- 不用不同家教形象区分主题；
- 可使用原创图形、色点、书脊或符号帮助区分，但不能制造过多颜色噪声；
- 用户可以自由切换，多头思考，但推荐顺序保持清楚。

### 9.4 聊天区

- 当前主题标题；
- 页头只保留标题、当前主题与学习状态，不重复左侧工作区已经展示的品牌图标；
- 维塔作为统一家教头像或角色；
- 通用文字输入、麦克风、发送和失败重试；只有当前存在诊断主问题时才显示提示与答案；
- 没有任务时上传入口位于输入区；
- 输入内容发送前可编辑，文案使用“消息”而不是把所有输入称为“回答”；
- Enter 直接发送，Shift+Enter 换行；发送后立即清空输入区，失败时恢复原草稿；
- 长对话只滚动消息区，输入区始终固定在当前视口底部；回复期间保持辅助操作布局稳定，不临时增加停止控件；
- 顶部“主题”与输入区“给我提示”“看答案”使用同一组低干扰辅助按钮外观；
- 支持长消息、自动滚动、回到最新消息和网络失败恢复；
- 不把聊天排成题目卡或考试卷。

### 9.5 右侧诊断区

- 桌面端由对话顶部的进度入口按需展开，不作为永久右栏；移动端使用底部抽屉；
- 材料整体：完成主题数、总主题数、进度条；
- 当前主题：主题名称、状态、0—100 分；
- 仅此两组核心信息，避免额外卡片堆叠。

### 9.6 移动端

- 默认全屏当前主题聊天；
- 左上角打开任务抽屉；
- 点击顶部主题名称打开模块化主题抽屉；
- 右上角打开材料与当前主题的诊断底部抽屉；
- 报告为独立全屏页面；
- 提示与答案放在输入框上方的紧凑操作条；
- 麦克风和发送位于输入框两侧；
- 处理软键盘、安全区域、返回行为、横竖屏和长文本；
- 当前主题完成后提供低干扰的下一个主题入口，不弹完成卡片。

## 10. 视觉与品牌

### 10.1 视觉原则

- 整体必须美观、精致、原创、有个人风格；
- 当前设计语言按约 80% 多邻国式学习体验与 20% Apple 式克制组合：前者负责圆润、鲜活、任务尺寸、粗壮图标、实体按钮、角色参与和即时反馈，后者负责排版、留白、信息层级和减少噪音；
- 多邻国只作为系统能力参考，不逐像素复制其商标、专属字体、角色、图标、配色数值或页面结构；
- 主按钮必须具有可见底层与明确 pressed 状态；次要操作不得与主操作使用相同的尺寸、色彩和立体强度；
- 图标统一使用圆润、粗壮、低细节的原创几何语言，小尺寸下仍可辨认，点击区域大于可见图形；不混用细线图标库，不泛用星光、魔法棒和大脑图标；
- 当前问题、回答区和唯一主操作形成最大的尺寸层级；分数、历史和辅助状态明显弱化，不用多个同尺寸卡片平均分配注意力；
- 每个核心色建立浅、中、深三层；品牌、主要操作、选中、成功、警告和错误分别承担固定语义，不用彩虹式装饰；
- Apple 式克制用于减少不必要卡片、标签、说明和重复反馈，不把界面做成苹果官网；
- 圆角、粗描边、几何图形、留白和孟菲斯元素只是审美参考，不是必须逐项套用的清单；
- 桌面三大区域的语义关系固定，具体呈现可使用按需浮层；色板、细节、比例和动效应依据真实页面效果自主设计；
- 禁止通用 AI 仪表盘式紫色渐变、玻璃拟态和随意堆卡片；
- 首版不做深色模式；满足 WCAG AA 对比度、键盘操作和 `prefers-reduced-motion`。
- 主界面统一使用一套无衬线字体栈；标题依靠字号和字重建立层级，不混用装饰性字体。
- 核心对话区域使用纯白背景，任务侧栏使用极浅暖白；暖色只服务于品牌、用户消息与主要动作，鼠尾草绿服务于维塔消息、选中与进度，避免蓝色等额外主色抢夺焦点。
- 空任务状态只保留一个主要上传动作，不在上传按钮下重复展示材料传输与本地保存说明；主题区和诊断区不在无数据时占据空间，也不重复展示 0 / 0、未开始和 0 分。
- 主要结构依靠排版与留白，不依靠大量边框和同尺寸卡片；仅输入区、消息气泡和必要浮层使用柔和轮廓。
- 正文默认 15—16px，辅助文字不小于 12px；同级标题、按钮和输入控件保持一致字号与字重，中文正文避免过粗。

视觉实现必须经过至少 1440×900 和 390×844 截图检查与迭代；不能只看代码宣布美观。

### 10.2 卡皮巴拉“维塔”

- Veritas 唯一学习伙伴和品牌吉祥物；
- 核心人格：专业但亲切、好奇、耐心、不评判，喜欢整理混乱信息、发现隐藏联系、把复杂事情变简单，并陪用户一步一步推进；它可以调皮、表达情绪和做不直接服务学习的小反应，但不靠卖萌喧宾夺主；
- `references/vita-final-reference.png` 是角色造型、服装、比例、面部、品牌符号、关键动作和聊天头像的唯一最高优先级参考；运行时资产必须直接从该母版提取，旧有探索图和二次生成图不再决定最终形象；
- 维塔固定为暖棕色卡皮巴拉，使用圆润但轻盈的体型、开朗亲和的表情、浅鼠尾草绿外套、奶油色小背包、星星挂件，以及橙色描边的“打开的书 + 小芽”徽标；
- 角色采用温暖手绘、深可可轮廓、柔和纸张颗粒与低饱和配色；聊天头像、空状态和学习动作必须保持同一五官、服装与视觉重量；
- 首版优先做少量可复用状态：默认、等待上传、处理中、给提示、鼓励、展示答案、错误；
- 聊天中维塔在左侧通过头像持续存在；用户在右侧。处理中、提示、正确和错误状态应切换对应表情或动作，并可在少数节点插入一句轻量人格反馈，例如“维塔把线索排整齐了”；
- 不为每个主题设计不同角色；
- 动效轻微，不用高频跳动打断学习；
- 不建立与核心流程无关的复杂角色资产系统。

参考资料见 `references/README.md`，它们只用于理解信息架构和插画气质。

## 11. 文件、数据、语音与本地边界

### 11.1 文件限制

- 一项任务一个文件；
- 单文件最大 30MB；
- PDF/PPTX 最多 200 页或幻灯片；
- 扫描 PDF 最多 OCR 50 页；
- 最终解析纯文本最多 30 万字符；
- DOCX 使用解压后体积、条目数和文本长度限制，不承诺页数；
- 超限时保留失败任务并明确建议拆分材料。

### 11.2 解析建议

| 格式 | 方式 |
|---|---|
| Markdown / TXT | 浏览器原生文本读取 |
| DOCX | Mammoth 提取纯文本或受控结构，不渲染原始 HTML |
| PPTX | JSZip + 安全 XML 解析 |
| 文本 PDF | PDF.js 逐页提取 |
| 扫描 PDF | PDF.js 渲染后 Tesseract.js OCR |
| 图片 | Tesseract.js OCR |

### 11.3 持久化与隐私

- 原文件 Blob、解析文本、知识地图、聊天、状态、草稿和报告存入 IndexedDB；
- 同一浏览器和设备可以恢复，清除浏览器数据会丢失；
- 原始文件不永久上传服务器；
- 只有必要的解析文本和当前 Agent 上下文发送给本地服务端代理与 DeepSeek；
- 服务端不保存材料和聊天；

### 11.4 语音

- 使用浏览器 SpeechRecognition；
- 点击麦克风开始，再次点击停止；
- 转写进入输入框，用户编辑后发送；
- 默认 `zh-CN`；
- 不支持、权限拒绝或识别失败时明确提示并保留文字输入；
- 不把音频发送给 DeepSeek，不额外接入第二家转写服务。

## 12. 模型调用

- 三个 Agent 全部使用 `deepseek-v4-flash`；
- Agent 1 分块提取、Agent 2 实时对话：显式关闭思考；
- Agent 1 覆盖审计、Agent 3 报告：允许 `reasoning_effort=low`；
- 首版不调用 Pro、不自动回退、不建立复杂模型路由；
- 使用 JSON Output 和 Zod；DeepSeek 客户端的一次调用只发送一次物理请求，所有网络重试和结构修复统一由 Agent Service 编排；
- 实时对话、阶段问题、提示和答案每个业务操作最多发起 2 次上游调用；材料分块提取、覆盖审计、首问和报告每个业务操作最多发起 3 次；
- 每个业务操作只有一个绝对截止时间，首次请求、退避等待和结构修复共享剩余时间，任何重试都不能重新获得完整超时；
- 429、500、503、明确可恢复网络错误、空内容和截断可在总预算内重试；结构失败只把脱敏后的 Zod 字段路径和允许形状反馈给下一次修复，不回传或记录模型正文；
- UI 只显示可理解错误，不暴露上游响应、推理内容、系统提示词和密钥；
- `.env.local` 已由用户提供真实 Key，开发时不得读取、输出、记录、截图或提交 Key；大部分测试使用 Mock，只运行少量真实 Agent 冒烟测试。

## 13. 安全

所有材料、用户消息和模型输出都视为不可信。运行时 Agent 没有执行代码、读取环境变量、访问其他任务、调用任意工具或请求任意网址的权限。模型只提出结构化语义判断，代码授权所有状态变化。

具体文件校验、提示注入、XSS、API 限制、秘密管理和安全测试见 `SECURITY.md`。任何为了赶进度而绕开安全边界的实现都不算完成。

## 14. 首版完成标准

只有同时满足以下条件才能宣布完成：

- 所有承诺格式均有真实样本完成解析并生成知识地图；
- 一份真实材料可以先进行通用讨论，再按需走完四层诊断、得分、报告、下载和分享；
- Agent 1 不使用固定 8 节点上限，覆盖归属无孤立核心条目；
- Agent 2 能覆盖通过、部分正确、错误、过短、复制、误解、偏题、不知道、提示、答案和连续无进展；
- 首次开场和多轮回复直接、具体、贴合材料与当前意图，不依赖固定禁词表或额外文案后处理；
- 刷新、关闭再打开和主题切换不丢状态；
- 桌面和移动端真实浏览器均完成主流程；
- 语音在支持的真实浏览器中可用，不支持时回退正确；
- 安全、构建、类型、lint、单元、组件、集成和端到端检查通过；
- 使用 `.env.local` 完成 Agent 1、Agent 2、Agent 3 的少量真实冒烟测试，过程无密钥泄漏；
- `README.md`、`PROJECT_MAP.md`、`DESIGN_GUIDE.md` 与真实代码一致；
- 本地生产构建可运行，不要求公网部署。
