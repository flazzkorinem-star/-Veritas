# 执行记录与真实性说明

## 冻结版本

- 分支：`codex/publish-a0691c1`
- HEAD：`bc2aac5832355e5caabc473a183311e5994a54d7`
- 任务 1 提交：`fb8f59f`
- 任务 2 提交：`bc2aac5`
- 任务 3、4：存在于未提交工作区
- 测试前后已跟踪差异指纹：`e8de2da52e8962bb8c946c9c0573c3355f7a67be`
- 正式模型：`deepseek-v4-flash`

## 执行过程

1. 新增独立回归脚本 `e2e/evaluation-bc2aac5-regression.spec.ts`，不修改旧版 `a0691c1` 证据。
2. 脚本先通过 Prettier、ESLint、TypeScript 和 Playwright `--list` 检查，确认仅有 20 个桌面 Edge case。
3. 第一次通过 `npm run test:e2e` 启动：生产构建成功，测试调度进程在任何 case 开始前退出，没有 Playwright case 输出、没有新样本。该次正式分母为 0，不计入产品成功或失败。
4. 单独运行 S01，真实链路 1 / 1 完成。
5. 在同一 HEAD 和工作区指纹上串行运行 S02—S20，19 / 19 完成。
6. S09 第一次 `RESPOND_TO_USER` 返回 502 `UPSTREAM_UNAVAILABLE`，页面出现“重试本轮”；回归脚本像真实用户一样点击该可见按钮，同一 session 随后恢复。失败请求、浏览器控制台 502 和重试动作均保留，没有删除后重跑 S09。
7. 没有因回答不理想而重跑 case 或挑选更好的模型输出。

## 结果完整性

| 证据              | 数量 |
| ----------------- | ---: |
| 原始 JSON         |   20 |
| 目标阶段截图      |   20 |
| 报告截图          |   20 |
| Markdown 报告     |   20 |
| Agent API 请求    |  256 |
| HTTP 200          |  255 |
| HTTP 502          |    1 |
| UI“重试本轮”      |    1 |
| 执行级 case error |    0 |

## 与真实用户不同的地方

- 输入由脚本按固定文本发送，点击和等待由 Playwright 执行；模型、前端、服务端、存储和报告均为真实实现。
- 使用 DOM 文件输入，没有打开操作系统文件选择器。
- 目标交互后，为得到最终报告而自动点击了 67 次“看答案”。这些点击不是用户原始测试意图，已单独分段。
- 本轮没有人工修改 IndexedDB；只读状态抓取不影响状态机。
- S01 与 S02—S20 分两条测试命令执行，但版本、生产构建方式、浏览器项目和证据脚本一致。
- 服务端内部模型重试次数没有进入浏览器证据结构，不能从 20 份 JSON 精确量化；只统计可以审计的 HTTP 和 UI 重试。

## 明确排除

以下内容不计入正式 20-session 分母：

- 任务 3 既有回归中人工写入 IndexedDB 的固定问题测试；
- 任务 4 既有回归中的直接 `/api/agents` 调用；
- 确定性 Mock 测试和内部函数单测；
- 第一次仅完成生产构建、没有启动任何 case 的调度尝试；
- 旧版 S01/S16 的历史重试和旧证据目录。
