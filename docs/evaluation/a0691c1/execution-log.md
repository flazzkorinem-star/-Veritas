# 执行过程与异常记录

## 版本

- 用户指定版本：`a0691c1`
- 实际运行提交：`8d52fb4`
- 两者 Git tree：`6842ceaa9a1599ac0ba49d76e836b3e57422086b`
- 结论：源码树逐字一致；`8d52fb4` 是发布该源码树的历史整理提交。

## 环境诊断

1. 首先尝试在内置浏览器上传文件。文件选择器返回但页面没有收到 change 事件，任务未创建；这不是 Veritas 失败，未计入正式 session。
2. 在 `next dev` 上运行新旧两条 Edge 上传用例，两者都停留空工作区。项目权威验收入口要求本地生产构建，因此这两次不计产品结果。
3. 本地生产构建成功。沙箱内生产用例上传正常，但固定 DeepSeek 上游三次均为 `UPSTREAM_UNAVAILABLE`，页面返回受控 503；没有读取或输出密钥。
4. 同一生产用例获准在沙箱外运行后，Agent 1 提取、审计和首问全部 200，确认是沙箱网络限制。
5. S01 第一次正式链路跑通，但 Playwright 临时附件未长期保留；随后只修改证据落盘方式并重跑 S01。正式分母使用有完整 JSON/报告/截图的后一次结果。
6. S16 第一次执行时 harness 没有先打开桌面“主题”浮层，第二主题 DOM 存在但不可见。失败 JSON 和截图保存在 `execution-issues/`；修正用户点击步骤后重跑并完成，只有后一次计入正式 S16。

## 正式批次

| 批次 | Session | 结果 |
|---|---|---|
| 1 | S01 | 1 / 1 技术链路完成 |
| 2 | S02—S05 | 4 / 4 技术链路完成 |
| 3 | S06—S10 | 5 / 5 技术链路完成 |
| 4 | S11—S15 | 5 / 5 技术链路完成 |
| 5 | S16—S20 | S17—S20 首次完成；S16 修正 harness 后完成 |

正式 20 条均保存原始证据。测试框架的“passed”只表示链路执行到报告，不代表人工产品评价为 PASS。
