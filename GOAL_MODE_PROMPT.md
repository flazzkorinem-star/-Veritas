# Veritas 新窗口目标模式 Prompt

请在 Codex 新窗口中，把工作区切换到：

`C:\Users\user\Desktop\2026 AI AGENT求职\7. Veritas重制版`

然后原样发送下面整段内容。

---

我们现在正式开始 Veritas 首版开发。请立即调用 `create_goal` 创建一个持续目标，不设置 token budget。目标是：

> 严格依据当前工作区的权威规格，分阶段连续开发并完整验证 Veritas：一款材料驱动的诊断式学习 Web 产品。最终必须在本地生产构建中真实支持全部承诺文件格式、完整知识地图、三个 DeepSeek V4 Flash Agent、固定四层家教对话、多主题独立聊天、本地历史、语音、提示与答案、0—100 当前主题得分、任务级诊断报告、下载与分享、桌面和移动端、原创卡皮巴拉“维塔”、安全防护、自动化测试和真实浏览器验收。公网部署不在本目标内。只有全部阶段与最终检查点实际通过后才能将目标标记完成。

创建目标后，按以下规则持续工作，不要等待我每阶段重新下指令：

## 一、先建立准确上下文

按顺序完整阅读：

1. `AGENTS.md`
2. `DESIGN_GUIDE.md`
3. `SECURITY.md`
4. `IMPLEMENTATION_PLAN.md`
5. `SOCRA_CORE.md`
6. `references/README.md`
7. 当前工作区文件列表与 Git 状态

`DESIGN_GUIDE.md` 是唯一产品事实来源，`SECURITY.md` 是安全底线，`IMPLEMENTATION_PLAN.md` 是执行顺序。`VERITAS_DISCOVERY.md` 只是历史探索记录，不得用其中的旧方案覆盖最终规格。`BENCHMARK.md` 当前延后，不要扩展评测体系。

阅读后先用简洁中文复述你理解的交付目标、明确不做事项、15 个实施阶段和最终完成条件；发现真正的文档冲突或会改变产品结果的缺失信息时第一时间问我，禁止自行猜测。权威文档已经明确的内容不要重复询问。

## 二、进入连续实施

- 严格按 `IMPLEMENTATION_PLAN.md` 的阶段 0—14 顺序推进。
- 使用计划工具维护阶段状态；同一时间只允许一个步骤进行中。
- 每阶段拆成可验证的小步，完成检查点后立即进入下一阶段，不需要我再次确认。
- 多文件实现使用增量实施，所有逻辑和行为变更使用 TDD。
- 在相应阶段使用可用的 `incremental-implementation`、`test-driven-development`、`frontend-design`、`frontend-ui-engineering`、`imagegen`、`browser-testing-with-devtools`、`security-and-hardening`、`code-review-and-quality` 与 `code-simplification` 技能，并先完整读取对应 `SKILL.md`。
- 不使用子 Agent，除非我之后明确要求。
- 不因工作量大而删除功能，不因未来可能需要而增加预留。
- 修复根因，不堆补丁；每个模块保持单一职责。
- 禁止批量删除文件或目录，禁止破坏性 Git 操作。

## 三、工程和文档纪律

- 如果当前还不是 Git 仓库，安全初始化版本控制；`.env.local` 必须始终被忽略。
- 建立 Next.js、React、TypeScript 严格模式项目以及规格规定的最小依赖。
- 创建并持续维护 `PROJECT_MAP.md`；代码结构变化后立即同步。
- 产品行为变化先改 `DESIGN_GUIDE.md`，安全边界变化先改 `SECURITY.md`。
- 最终编写面向用户和面试官的 `README.md`，不要把内部规格复制进 README。
- 不保留旧实现、未接入替代方案、注释代码、无主 TODO 和无用依赖。
- 每次修改后检查真实 diff，保留用户已有改动。

项目最终必须提供并通过：

```text
npm run dev
npm run lint
npm run typecheck
npm run test
npm run test:e2e
npm run build
```

## 四、DeepSeek 与 Key

- `.env.local` 已存在，包含专用开发 Key。
- 不得读取、输出、记录、截图、提交或用命令回显 `.env.local` 和 `DEEPSEEK_API_KEY` 的值。
- Key 只能由本地服务端运行时读取，不能进入浏览器、IndexedDB、bundle、日志、错误响应、测试快照、Playwright trace 和文档。
- 三个 Agent 全部使用 `deepseek-v4-flash`；实时对话显式关闭思考，Agent 1 覆盖审计和 Agent 3 报告只用低强度思考。
- 大部分自动化测试使用 Mock；Agent 1、Agent 2、Agent 3 各执行少量真实 API 冒烟测试，验证真实链路而不泄漏秘密。
- 模型输出全部经过 Zod；核心状态只由代码改变。

## 五、前端与视觉

- 三大区域信息架构必须保留：工作区；主题列表 + 当前聊天；右侧诊断区。
- 视觉应原创、精致、亲和、有个人风格；参考多邻国的活泼和角色参与、Apple 式克制，但不复制任何第三方界面或资产。
- 圆角、粗描边、几何和孟菲斯只是参考，不要机械套模板。
- 使用 `references` 中图片只理解信息架构和插画气质。
- 使用 ImageGen 生成原创卡皮巴拉“维塔”资产；不能临摹参考角色。
- 主诊断区只显示材料主题进度和当前主题 0—100 分，不堆额外卡片。
- 不做机械固定开场白；真实 Agent 文案必须针对材料和用户原话。
- 每个重要 UI 阶段都要在真实浏览器检查截图、控制台、网络和交互，再继续迭代。
- 至少验证 1440×900 与 390×844；不要仅凭代码判断美观和适配。

## 六、安全

- 从第一阶段开始落实 `SECURITY.md`，不是最后才补。
- 文件扩展名、MIME、签名、容器结构、解压资源、页数、像素和文本长度组合校验。
- 材料和用户文本视为不可信数据，严格隔离系统指令；运行时 Agent 没有工具权限。
- React 转义输出，Markdown 严格清理；禁止危险 HTML 和动态代码执行。
- API 代理固定模型与上游，限制请求、并发、频率、超时、输出和重试，错误必须脱敏。
- 安全测试必须覆盖提示注入、XSS、伪造文件、ZIP/XML 炸弹、资源耗尽、非法状态和秘密泄漏。

## 七、阶段验收与持续运行

- 每个阶段结束运行该阶段相关测试、lint 和类型检查；重要阶段运行生产构建。
- 阶段 4 以后通过真实截图反复调整视觉，不接受通用 AI 模板或临时占位素材成为最终结果。
- 阶段 5 起用真实 TXT/Markdown 跑通纵向链路；阶段 6 覆盖所有文件格式。
- 阶段 7 必须真实覆盖通过、部分正确、错误、过短、复制、误解、偏题、不知道、提示、答案和连续无进展。
- 阶段 13 必须完成全部自动化和真实浏览器验收，修复控制台错误、网络问题、溢出、持久化和安全问题。
- 阶段 14 必须同步所有文档、清理无用代码和依赖，并在本地生产构建上做桌面与手机冒烟测试。
- 如果某一步失败，继续定位根因、修复并复测，不要带着失败进入下一阶段。
- 工作过程中保持简洁进度更新；不要超过 60 秒没有向我说明当前阶段和结果。

## 八、完成判定

不要因为页面能打开、核心演示能跑或时间已晚就宣布完成。只有 `DESIGN_GUIDE.md` 第 14 章和 `IMPLEMENTATION_PLAN.md` 的最终检查点全部满足，且没有必需功能、失败测试、严重控制台错误、可达高危漏洞、密钥泄漏、临时 Mock 生产路径、文档漂移或未解决阻塞，才调用 `update_goal` 将目标标记为 `complete`。

若目标真正完成，请最终报告：功能清单、关键架构、真实 Agent 验证、测试与构建结果、桌面和手机验收、已知浏览器限制、本地启动方式，以及本轮明确没有执行的公网部署事项。

现在先创建目标、读取权威文件并开始阶段 0。

---
