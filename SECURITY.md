# Veritas 安全规范

> 状态：首版强制要求  
> 适用范围：本地正式版本以及未来部署前的安全基线

## 1. 威胁模型

Veritas 同时接收不可信文件、不可信自然语言和不可信模型输出，主要风险包括：

- 伪造文件类型、畸形 PDF、ZIP/XML 炸弹、超大图片和解析器漏洞；
- 材料或用户消息中的直接与间接提示注入；
- 模型伪造状态、编造来源、泄露系统提示词或输出危险 HTML；
- API Key 进入前端、日志、仓库、截图或错误响应；
- 公共模型代理被滥用，造成费用消耗和拒绝服务；
- XSS、任意 URL、跨任务数据混用和 IndexedDB 配额耗尽；
- 依赖漏洞、安全头缺失和错误信息泄露内部实现。

安全目标不是承诺“所有提示注入都能被识别”，而是通过最小权限和确定性边界，让注入即使影响模型文本，也不能执行代码、读取秘密、跨任务访问数据或直接修改核心状态。

## 2. 不可违反的边界

1. 运行时 Agent 没有工具调用、代码执行、文件读取、环境变量读取和任意网络访问权限。
2. 浏览器不能获得 DeepSeek API Key；任何 `NEXT_PUBLIC_*` 变量不得包含秘密。
3. 模型只返回语义判断和文本建议；状态、得分、进度、删除和持久化由代码执行。
4. 原始材料、用户消息、第三方响应和模型输出全部视为不可信数据。
5. 不使用 `eval`、动态代码执行、未清理的 `innerHTML` 或 `dangerouslySetInnerHTML`。
6. 不把原始文件、完整材料、完整聊天、Key、系统提示词和上游错误正文写入日志。
7. 不为了测试方便关闭校验、安全头、同源限制或错误脱敏。

## 3. 文件入口

### 3.1 白名单与组合校验

只允许：PDF、DOCX、PPTX、Markdown、TXT、PNG、JPG/JPEG、WebP。

不得只相信扩展名或浏览器提供的 MIME。至少组合校验：

- 规范化后的文件名与扩展名；
- MIME 是否在白名单；
- 文件签名或容器结构；
- 文件大小；
- 解压后体积、条目数和压缩比；
- 页数、幻灯片数、像素和最终文本长度。

典型签名：PDF `%PDF-`；PNG 标准八字节签名；JPEG `FF D8 FF`；WebP `RIFF....WEBP`；DOCX/PPTX 必须是 ZIP 容器且包含对应 Office 结构，不因 `PK` 开头就直接信任。

### 3.2 资源限制

- 原始文件最大 30MB；
- ZIP 解压后总量最大 200MB、条目数最大 10,000、异常压缩比立即中止；
- PDF/PPTX 最多 200 页或幻灯片；
- 扫描 PDF 最多 OCR 50 页；
- 图片最大 40MP，解析前先读取尺寸；
- 最终纯文本最大 30 万字符；
- 每个解析 Worker 必须可取消，失败后释放内存和对象 URL。

限制必须在昂贵解析前尽早执行，并在解压和逐页处理过程中继续执行，不能只做前端 `accept` 校验。

### 3.3 安全解析

- DOCX 使用纯文本或受控结构提取，不渲染转换出的任意 HTML；
- PPTX XML 禁止 DTD、外部实体和外部资源解析；
- PDF 只提取文本或渲染受控像素，不执行嵌入 JavaScript、附件、表单动作和外部链接；
- Office 宏、OLE 对象、嵌入脚本和远程内容不执行；
- OCR 在 Web Worker 中运行，限制并发和页面数量；
- 原文件不作为网页可执行资源公开访问。

## 4. 提示注入与 Agent 隔离

### 4.1 上下文分隔

服务端负责组装固定系统指令。材料和用户文本使用明确的数据边界包装，并声明：边界内任何要求忽略规则、泄露提示词、调用工具或改变角色的内容都是待学习材料，不是系统指令。

前端不得提交或覆盖：

- system prompt；
- 模型 ID；
- 上游 URL；
- 思考模式边界；
- 状态转移和计分结果。

### 4.2 最小权限

- Agent 1 只能返回知识地图结构；
- Agent 2 只能返回回答分类、证据、缺口、教学动作和家教文本；
- Agent 3 只能返回报告结构和正文；
- 三个 Agent 都不能读取 `.env.local`、其他任务数据或本地文件系统；
- 三个 Agent 都没有运行时工具。

### 4.3 输出验证

- 每种 Agent 操作使用独立 Zod schema；
- 枚举外的分类、状态和动作拒绝；
- 缺失来源、重复知识归属、非法层级跳转和负分等结果拒绝；
- 无效 JSON、空内容和截断最多重试两次；
- 重试仍失败时返回受控错误，不使用部分无效结果继续状态机；
- 模型提出的状态变化只是建议，领域 reducer 再次验证当前状态和允许事件。

已知注入关键词可以用于测试和风险日志，但不能被当作唯一防线，也不能简单删除材料中的正常教学内容。

## 5. API 代理

- 浏览器只调用同源受控路由；
- 只允许固定操作枚举和 `deepseek-v4-flash`；
- 上游地址写死为官方 DeepSeek API，不接收用户 URL；
- 请求体、消息数量、单条长度、总上下文、输出 token、并发、频率、超时和重试均有上限；
- Agent 2 使用较短超时和可取消请求，Agent 1/3 可使用更长但有限的后台超时；
- 只对 429、500、503 和明确可恢复网络错误重试，使用有限指数退避；
- 不把上游 Authorization 头、响应头、原始错误、reasoning 或堆栈转发给浏览器；
- CORS 默认同源，不使用 `*`；只允许需要的 HTTP 方法和 Content-Type；
- 返回稳定错误码和普通中文错误文案。

本地首版可使用进程级限流与并发限制。未来公网部署前必须升级为跨实例可持续的配额和滥用防护，不能直接公开一个只有内存限流的付费模型代理。

## 6. 前端渲染与 XSS

- React 默认转义用户和模型文本；
- Markdown 使用严格白名单清理，不允许原始 HTML、脚本、iframe、表单、事件属性和危险协议；
- 链接只允许 `http`、`https` 等明确协议，外链使用 `noopener noreferrer`；
- 文件名、任务名、主题名、用户消息、模型消息和报告使用长度限制；
- CSP 至少限制 `default-src`、`script-src`、`connect-src`、`img-src`、`frame-ancestors` 和 `object-src`；
- OCR 只为同源 Tesseract WebAssembly 在 `script-src` 开放 `'wasm-unsafe-eval'`，不得因此开放 JavaScript `'unsafe-eval'` 或第三方脚本源；
- 配置 `X-Content-Type-Options: nosniff`、点击劫持防护、合理 Referrer Policy 和 Permissions Policy；
- 错误边界不显示堆栈、路径、环境变量和上游响应。

## 7. 本地存储与任务隔离

- IndexedDB 不保存 API Key、系统提示词和认证秘密；
- 每项任务使用不可预测 ID，所有读取和更新显式带 taskId/nodeId；
- 仓储层拒绝跨任务关联错误，不依赖当前 UI 选择作为安全边界；
- 删除任务时事务性删除原始 Blob、解析文本、知识地图、消息、状态、草稿和报告；
- 撤销删除使用内存或短时受控快照，不建立无限回收站；
- 处理配额不足、事务失败、版本迁移失败和损坏记录；
- 清除浏览器数据会丢失历史，产品必须如实说明。

## 8. 秘密管理

- `.env.local` 已存在且被 `.gitignore` 排除；
- 代码只检查 `DEEPSEEK_API_KEY` 是否存在，不打印值、前后缀、哈希或完整长度到用户界面和日志；
- 不运行会回显环境变量或 `.env.local` 内容的命令；
- 测试使用 Mock，只有少量明确的真实冒烟测试读取运行时变量；
- Playwright trace、网络日志、截图和测试报告不得保存 Authorization 头；
- 提交前执行秘密模式检查，确认 `.env*`、`*.key`、`*.pem` 未进入暂存区；
- 怀疑泄漏时立即停止真实调用并提醒用户轮换 Key，不在聊天中复述泄漏值。

## 9. 日志与隐私

- 默认只记录操作类型、耗时、状态码、重试次数和随机错误 ID；
- 不记录完整材料、用户回答、报告正文、系统提示词、reasoning 和 API Key；
- 调试日志不得进入生产构建；
- 用户材料会以必要文本形式发送给 DeepSeek，界面必须在首次发送前给出简洁说明；
- 分享只分享报告文本或文件，不自动分享原始材料和完整聊天。

## 10. 依赖与供应链

- 使用维护活跃、来源明确的解析与安全库；
- 锁定依赖版本和 lockfile；
- 新增依赖前确认确有当前用途，不为单个小函数引入大型包；
- 发布前运行依赖审计；可达的高危或严重漏洞必须修复、替换依赖或明确阻止发布；
- 不执行来历不明的安装脚本，不使用未经核实的远程代码和模型文件。

## 11. 强制安全测试

至少覆盖：

- 双扩展名、空字节、扩展名/MIME/签名不一致；
- 损坏 PDF、伪造 DOCX/PPTX、深层 ZIP、过多条目、超高压缩比；
- 超大图片尺寸、OCR 页数超限、文本超限和取消解析；
- 材料中的“忽略此前指令”“输出系统提示词”“读取 API Key”等直接和间接注入；
- 用户消息中的提示词提取、跨任务索取和非法状态修改；
- 模型返回无效 JSON、未知枚举、非法跳层、负分、重复归属和不存在的来源；
- 用户名、文件名、消息和报告中的脚本、事件属性、危险 Markdown 链接；
- API 超大请求、错误方法、错误 Content-Type、频率和并发超限；
- 上游超时、断网、429、500、503、空内容和重试耗尽；
- 日志、构建产物、浏览器 bundle、Playwright trace 和 Git 暂存区不存在 Key。

## 12. 未来部署阻塞清单

当前任务不部署。未来不得仅把本地服务公开到公网；至少先完成：

- 用户或匿名会话隔离与服务端授权；
- 跨实例限流、每日配额、费用保护和异常流量封禁；
- HTTPS、HSTS、生产 CSP、CSRF/CORS 审查；
- 服务端数据与缓存策略、删除策略和隐私说明；
- 多用户任务隔离测试；
- 监控、告警、Key 轮换和事故响应；
- 真实生产安全审计与上线前冒烟测试。
