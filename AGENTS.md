# Veritas 开发规则

所有输出、文档、提交说明和用户可见文案使用中文。

## 开始前

1. 读本文件。
2. 读 `DESIGN_GUIDE.md` 中与任务有关的产品规则。
3. 涉及文件、模型、API、存储、用户输入或渲染时，读 `SECURITY.md` 的相关章节。
4. 修改代码前，读 `PROJECT_MAP.md`、目标源码、测试和相邻实现。

权威顺序是：用户最新明确决定 > `DESIGN_GUIDE.md` > `SECURITY.md` > `PROJECT_MAP.md` > 代码现状。发现冲突或缺少会改变产品结果的信息时，先问用户；已有明确答案的内容不要重复询问。

## 工程原则

- KISS：选择最短、最直白且能验证的实现。
- YAGNI：不增加当前版本用不到的功能、抽象、配置、兼容层或预留。
- 单一职责：模型、状态机、存储、解析和 UI 不互相越权。
- 修根因，不为单个样例堆补丁，不过拟合测试。
- 尊重已有用户改动，只修改当前任务所需范围。
- 不使用破坏性 Git 命令，不批量删除未明确确认的文件。
- 不留下旧实现、注释代码、无主 TODO 或未接入的替代方案。
- 能用平台能力或现有依赖解决时，不新增依赖。

## 模型与安全边界

- 三个 Agent 统一使用 `deepseek-v4-flash`，当前版本不使用 Pro。
- 模型负责语义理解、自然教学和报告生成；代码负责顺序、状态、计分、权限、校验、完成和持久化。
- 所有结构化模型输出必须通过 Zod。自由文本不能直接修改核心状态。
- 材料、用户消息和模型输出都是不可信数据。
- 运行时 Agent 不得执行代码、读取环境变量或文件系统，也不得访问任意网络工具。
- 严格遵守 `SECURITY.md`，文件、API、模型输出和 Markdown 都要在边界校验或清理。
- 不读取、输出、记录、截图或提交 `.env.local` 与 `DEEPSEEK_API_KEY` 的值。
- Key 只由本地服务端运行时读取，不能进入浏览器 bundle、IndexedDB、日志或测试产物。

## 实施与验证

- 多文件或行为变更先写最小计划，再分步落地。
- 逻辑和行为变更遵循 TDD：先写失败测试，再写最小实现，最后重构。
- 每个小步运行相关测试。完成前至少运行：

```text
npm run lint
npm run typecheck
npm run test
npm run build
```

- 浏览器功能还要运行 `npm run test:e2e`。
- UI 变更必须在真实浏览器中检查 1440×900 和 390×844，确认控制台、网络和溢出正常。
- 确定性 Agent 测试默认使用 Mock；真实 DeepSeek 测试只在明确开启时运行，且不能暴露 Key。
- 不能用“代码已写完”代替验收，也不能为了缩小任务而删除规格功能。

## 产品与文档

- 三大区域的信息架构固定，视觉应清晰、亲和、克制，不复制第三方资产或界面。
- 用户文案要让学生一眼看懂，避免内部工程和认知科学术语。
- 不硬编码机械开场白，不使用通用 AI 仪表盘模板。
- 产品行为变化先更新 `DESIGN_GUIDE.md`；安全边界变化同步 `SECURITY.md`；代码职责变化同步 `PROJECT_MAP.md`。
- `README.md` 只承担项目介绍、运行入口和导航，不复制内部规范。
- 同一事实只保留一个权威来源，其他文档使用链接。

## 当前范围

当前版本只要求本地正式运行。账号、云同步、多人协作、完整游戏化、复杂知识图谱和公网部署均不在范围内。
