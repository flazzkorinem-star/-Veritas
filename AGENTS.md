# Veritas — AI 开发者作业规则

AI 编码代理在本仓库的作业契约。所有输出使用中文。本文件只管"怎么工作"，产品规格不在这里。

## 真相源（先读再动）

- `docs/design-guide.md` = 产品唯一真相源（产品、流程、三个 Agent、评分、报告、UI）。
- `docs/project-map.md` = 代码唯一真相源（模块结构、数据流、文件职责）。
- 三份文档互补、不重复；各自只写自己负责的部分。
- 与真相源冲突就停下：描述冲突，询问改需求/改文档/改代码，不自行其是。
- 真相源未覆盖的情况也停下，问清楚再动手。

## 第一纪律：只做用户要求的

- 单一职责：每个模块职责要能一句话说清楚。
- 有歧义就主动问，不要自己猜。
- 只实现用户明确要的。**没说=不做**，不预判、不顺手加功能 / 抽象 / 配置 / 防御分支 / 字段。
- 外科手术式修改：只动任务需要的文件，不重构、不清理无关代码。
- 减法优先：能删则删，新增任何东西都要有"当前就用得到"的 理由。
- 简单优先：遵守 KISS / YAGNI，不加当前用不到的抽象、配置和预留能力。

> 不靠列举"禁止做的功能"来防漂移，靠"只做被要求的"。

## 工程约束

- **确定性归代码，语义归 Agent**：LLM 只负责语义判断；分数、层级推进、按钮点击、轮次控制等确定性状态由代码维护。
- **prompt 做减法**：不为每个新对话场景加「遇到 X 就说 Y」或「不准 X」。用目标 + 人设约束模型，靠常识泛化。
- **API route 是薄转发层**：只解析请求、调 Agent、返回、映射错误码；清洗 / 校验 / 归一化放 `lib/agents/*.ts` 的 parse 函数，前端响应解析用 `lib/apiResponse.ts`。
- **测试**：只为确定性逻辑写测试（评分计算、层级推进、parse / 归一化、支持记录）；LLM 对话质量和 prompt 措辞不写单元测试。

## 安全（碰之前先停下问用户）

- **删除**：禁止批量删除。不用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。一次只删一个明确路径：`Remove-Item "C:\path\to\file.txt"`。要批量删就停下，让用户手动做。
- **密钥**：不修改 `.env.local`，不打印密钥真实值。
- **依赖**：保留 `@napi-rs/canvas`（`lib/pdf.ts` 当前直接使用它为 PDF 解析补齐 `DOMMatrix`、`ImageData` 和 `Path2D`）。
- **环境变量**：`DEEPSEEK_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL_FAST`。

## 验证（声称完成前必须跑）

- 改完跑 `npm run test:run`；声称生产可用前跑 `npm run build`（Windows 用 `npm.cmd run ...`）。
- 命令因 sandbox `spawn EPERM` 失败：用批准的提权方式重跑同一命令，不绕过测试。

## 文档同步（任务结束前）

检查是否改变了文档相关事实，只更新受影响部分：

| 文件 | 何时更新 |
|---|---|
| `AGENTS.md` | 工作纪律、安全、验证规则变化 |
| `docs/design-guide.md` | 产品规格、流程、Agent 职责、评分、报告、UI 变化 |
| `docs/project-map.md` | 模块结构、数据流、文件职责变化 |
| `README.md` | 用户可见功能、运行方式、部署方式、公开描述变化 |

最终回复列出改过的文档 + 一句原因；无需同步就写：`无需同步文档`。
