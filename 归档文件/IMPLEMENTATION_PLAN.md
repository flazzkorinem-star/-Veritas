# Veritas 连续目标模式实施计划

> 更新时间：2026-08-01  
> 状态：最终执行清单。新窗口目标模式必须按顺序实施并逐阶段验收。  
> 原则：同一个目标连续运行，多轮、分阶段完成；每个阶段都必须验证，不以“代码已经写完”代替真实可用。

## 一、交付目标

交付一个达到正式产品质量、在本地完整运行的本地优先单用户响应式 Web 产品。本轮不负责公网部署，但架构和安全边界不能阻碍后续部署。用户能够：

1. 上传受支持的真实材料；
2. 查看真实解析进度；
3. 自动获得具有诊断价值的知识节点；
4. 在多个节点聊天之间切换；
5. 通过记忆、理解、应用、分析四层完成诊断与学习；
6. 使用文字、语音、提示和答案功能；
7. 实时查看每个节点的进度、状态和 0—100 分；
8. 自动获得并下载任务级诊断报告；
9. 搜索、重命名、置顶、分享和删除历史任务；
10. 刷新或重新打开同一浏览器后恢复材料、节点、进度和聊天；
11. 在桌面和手机上完成完整主流程；
12. 通过自动化测试、真实浏览器验收和生产构建后在本地完整运行。

## 二、首版产品边界

### 必须实现

- 材料：PDF、DOCX、PPTX、Markdown、TXT、PNG、JPG/JPEG、WebP；
- Office 文件只支持现代 `.docx` 与 `.pptx`，不支持旧版 `.doc` 与 `.ppt`；
- 图片和扫描 PDF 的 OCR；
- DeepSeek V4 Flash 的真实调用；
- 三类 Agent 的真实链路；
- 四层固定诊断状态机；
- 三种完成状态：通过、提示后通过、给答案通过；
- 本地历史恢复；
- 搜索、重命名、置顶、分享、删除；
- 文字与语音输入；
- 自动诊断报告与下载；
- 桌面三大区域和手机端重组；
- 原创卡皮巴拉品牌角色“维塔”；
- 安全、错误处理、测试、文档和本地运行说明。

### 明确不做

- 用户账号、云端历史和跨设备同步；
- 多人协作；
- 每个节点不同家教角色；
- 复杂知识图谱和跨节点学习者画像；
- 节点或层级重做；
- 用户手动编辑 Agent 1 生成的节点；
- 笔记、练习等其他知识产物；
- 完整 benchmark、AI Judge 和自动语义回归平台；
- 公网部署、自定义域名和线上运维；
- 深色模式、国际化和完整游戏化；
- 未被当前功能调用的抽象、配置和兼容层。

## 三、推荐技术架构

### 应用栈

- Next.js 当前稳定版，App Router；
- React + TypeScript 严格模式；
- Tailwind CSS + CSS 变量设计令牌；
- Zod：环境变量、API、模型输出和持久化数据校验；
- Dexie：IndexedDB 本地持久化；
- Vitest + React Testing Library：单元与组件测试；
- Playwright：桌面、手机和完整流程测试；
- 不引入大型全局状态框架，核心状态使用领域 reducer 与小范围 Context。

### 本地优先数据边界

- 原始文件 Blob、解析文本、任务、节点、消息、状态和报告保存在 IndexedDB；
- 文件解析尽量在浏览器完成；
- 原始文件不永久上传服务器；
- 只有完成解析后的必要文本、节点上下文和对话上下文发送到服务端模型代理；
- 服务端不保存学习材料和对话；
- API Key 只存在于服务端环境变量；
- 历史恢复仅保证同一浏览器、同一设备；
- 首版“分享”分享诊断报告文本或文件，不创建包含原材料的公开任务链接。

### 文件解析

首版采用“一项任务一个文件”：单文件最大 30MB；PDF/PPTX 最多 200 页或幻灯片；扫描 PDF 最多 OCR 50 页；最终解析纯文本最多 30 万字符。DOCX 以解压后体积、条目数和文本长度限制，不伪造页数。超过限制时保留失败任务并明确建议拆分材料。

| 格式 | 推荐方式 |
|---|---|
| Markdown / TXT | 浏览器原生文本读取 |
| DOCX | Mammoth 在浏览器提取结构化文本 |
| PPTX | JSZip + XML 解析幻灯片文本与顺序 |
| 文本 PDF | PDF.js 提取逐页文本 |
| 扫描 PDF | PDF.js 渲染页面后交给 Tesseract.js OCR |
| 图片 | Tesseract.js OCR |

`.doc` 和 `.ppt` 是旧版二进制格式，首版只支持 `.docx` 与 `.pptx`。界面文案统一写“Word（DOCX）”和“PPT（PPTX）”，避免错误承诺。

### 进度反馈

- 文件读取：使用真实字节进度；
- PDF、PPTX、DOCX、OCR：显示已处理页数、幻灯片数、图片数或阶段；
- DeepSeek 节点提炼：显示“不定进度 + 当前阶段 + 已用时间”；
- 不为无法测量的模型推理伪造 37%、82% 等百分比；
- 任务失败后保留任务卡片，提供重试与删除。

## 四、DeepSeek 调用策略

首版三个 Agent 统一使用官方模型 ID `deepseek-v4-flash`，不调用 Pro，也不实现自动模型回退或复杂路由。

- Agent 1 分块提取：关闭思考模式；
- Agent 1 全局合并、去重与覆盖审计：开启思考模式，`reasoning_effort=low`；
- Agent 2 所有实时对话回合：显式关闭思考模式；
- Agent 3 报告：后台调用，可开启思考模式，`reasoning_effort=low`；
- 交互路径优先低延迟，非交互路径才允许有限推理；
- 使用官方 JSON Output 返回结构化结果；
- 所有结果经 Zod 校验；
- 空内容、JSON 不合法或截断时最多自动重试两次；
- 429、500、503 使用带抖动的指数退避；
- 不把历史 reasoning 内容重新发送给 API；
- 不向前端暴露模型原始错误、推理内容或密钥。

## 五、Agent 1：完整知识地图与分批诊断协议

### 目标

Agent 1 不能只返回少量“精选节点”。它必须先建立可追溯的材料知识地图，再把适合深度诊断的相关知识聚合为主题。用户应始终知道哪些内容已经诊断、尚未诊断，以及哪些内容只作为辅助材料被覆盖。

### 学习目标的建立方式

- 学习目标是 Agent 内部用于组织知识地图和问题的教学目标，不是要求用户先完成的产品配置；
- 不展示目标确认弹窗、目标卡片、固定用途选项或“调整目标”按钮；
- Agent 1 直接根据材料建立覆盖核心概念、易混点、应用方法与边界的内部目标，随后进入第一个节点；
- 用户不需要先想清楚自己属于考试、面试还是其他场景，重点直接落在对话本身；
- 用户在任何时候通过自然语言说明“我主要准备面试”“这部分不用考细节”等意图时，Agent 2 依据明确表达调整尚未开始节点的排序、例子和提问场景；
- 用户意图不明确时不主动弹出选择，也不把对话变成需求访谈；
- 后续调整不删除知识条目、不降低完整覆盖范围，也不改变已经产生的诊断证据。

### 两阶段模型流程

1. **分块提取**：Flash 非思考模式逐块识别材料结构、原子知识条目、来源位置、内容类型和潜在误区；
2. **全局整理与覆盖审计**：Flash 低强度思考模式合并同义项、消除重复、恢复跨章节关系、聚合诊断主题，并核对每个来源分段与知识条目的归属。

模型输出必须经过 Zod 校验。若覆盖审计发现孤立的核心知识、缺失来源或超大主题，只重试受影响阶段，不重新运行全部流程。

### 知识地图结构

```text
MaterialModule
  id
  title
  sourceRange

KnowledgeItem
  id
  moduleId
  title
  summary
  kind: CORE | SUPPORTING
  diagnosticRationale
  sourceReferences[]
  commonMisconceptions[]

DiagnosticNode
  id
  moduleId
  title
  objective
  knowledgeItemIds[]
  sourceReferences[]
  canonicalUnderstanding
  commonMisconceptions[]
  bloomTargets.memory
  bloomTargets.understanding
  bloomTargets.application
  bloomTargets.analysis
  order

CoverageAssignment
  knowledgeItemId
  disposition: DIAGNOSED_IN_NODE | SUPPORTING_IN_NODE | REFERENCE_ONLY
  nodeId?
  reason?
```

### 聚合与数量规则

- 不设“整份材料最多 8 个节点”之类的产品硬上限；
- 节点数量由材料中不可再合理合并的核心诊断主题决定；
- 一个诊断节点通常聚合 2—5 个高度相关的核心知识条目，但独立概念不能为了减少数量被强行合并；
- 案例、数据、延伸说明等辅助内容进入知识地图并绑定相关节点，不为每一条单独执行四层；
- 每个核心知识条目必须且只能归入一个主要诊断节点，避免重复诊断；
- 每个辅助条目必须绑定相关节点或以 `REFERENCE_ONLY` 明确说明不单独诊断的理由；
- 不能为了缩短对话而静默丢弃重要内容；
- 大材料通过模块折叠、节点排序和分次完成控制负担，而不是假装少量节点已经覆盖整份材料。

诊断价值建议按 100 分计算：

| 因素 | 权重 |
|---|---:|
| 对材料主旨的核心程度 | 30 |
| 容易产生误解或混淆 | 25 |
| 能否迁移到真实应用 | 20 |
| 是否是其他内容的前置基础 | 15 |
| 材料证据是否充分 | 10 |

### 行为规则

- 模块和节点标题短、可区分，避免直接复制长句；
- 相互重叠的候选必须合并；
- 节点之间不共享学习状态，也不建立复杂知识图谱；
- 排序时允许把关键前置概念提前；
- 每个节点必须绑定材料来源位置和短摘录；
- 用户不确认、不编辑节点；
- 节点生成完成后自动打开第一个节点；
- 一次只建议完成一个节点；节点结束后由用户选择暂停或继续，不自动把用户推入下一节点；
- UI 按材料模块折叠展示全部诊断节点，并持续显示已完成与尚未诊断数量；
- 无法提取文本或没有可靠节点时进入失败状态，不凭空生成。

## 六、Agent 2 唯一状态机

### 状态

```text
TaskStatus:
  PROCESSING | READY | IN_PROGRESS | COMPLETED | FAILED

NodeStatus:
  NOT_STARTED | IN_PROGRESS | COMPLETED

StageStatus:
  LOCKED | ACTIVE | PASSED | PASSED_WITH_HINT | PASSED_WITH_ANSWER
```

四层顺序固定：

```text
MEMORY → UNDERSTANDING → APPLICATION → ANALYSIS
```

### 每层流程

1. 进入层级，生成一个主问题；
2. 用户回答；
3. 模型判断回答类型、掌握证据、缺失点、误解和是否有进展；
4. 代码根据结构化判断执行状态转移；
5. 未通过时围绕同一个主问题追问，不更换考察目标；
6. 通过后给出具体反馈并进入下一层；
7. 分析层完成后将节点标记为完成并更新报告。

四层是后台教学状态机，不是要求用户学习的产品术语。聊天中默认不说“现在进入记忆层”“接下来是应用层”，右侧主诊断视图也不要求用户理解四层名称；只有诊断报告或必要的详细说明中才展示层级依据。

### 首次开场与真人感

- 不硬编码固定开场白，由 Agent 2 根据当前材料和第一个主题动态生成；
- 开场最多用一两句建立语境，随后提出一个边界清楚、容易接住的问题；
- 默认不宣读“整理出了多少主题”“将按什么模式带你”“可以调整目标”等系统说明；
- 禁止模板化的“我已经分析了你的材料”“我们将全面覆盖”“现在让我们开始”等 AI 助手口吻；
- 第一条消息和后续每轮都必须回应具体材料或用户原话，不能只替换知识点名称后复用同一段话；
- 对话文案质量在真实 Agent 冒烟测试和浏览器体验阶段继续迭代，不把任何示例句当成固定产品文案。

### 内部回答分类

以下分类只用于选择教学动作，不增加用户可见状态：

```text
CORRECT
PARTIAL
INCORRECT
TOO_SHORT
COPIED
MISCONCEPTION
OFF_TOPIC
NO_ANSWER
```

除 `CORRECT` 外均不通过，但下一动作不同：

- PARTIAL：指出已经正确的部分，只追问缺失点；
- INCORRECT：指出矛盾或给出更小问题；
- TOO_SHORT：明确还缺少哪一部分；
- COPIED：要求用自己的话重述，并更换表达角度；
- MISCONCEPTION：用反例、冲突场景或类比帮助自我修正；
- OFF_TOPIC：简短回应支线，再自然拉回原问题；
- NO_ANSWER：解释作答要求并主动给出可进入思考的案例或线索。

### 追问与自动答案规则

- 三轮是“连续无进展”的阈值，不是总追问硬上限；
- 模型为每次回答返回 `progress = ADVANCING | STALLED`；
- 用户出现新的正确理解时，连续无进展计数归零；
- 只要仍在推进，就允许超过三轮；
- 连续三轮 STALLED 后，Agent 自动给出完整答案；
- 自动答案与点击答案完全相同：当前层 `PASSED_WITH_ANSWER`、0 分、进入下一层；
- 用户主动点击答案时立即执行，不等待三轮。

### 提示规则

- 只有用户点击提示按钮才设置 `hasRequestedHint = true`；
- Agent 普通对话中主动给出的案例、类比和知识线索不改变最终状态；
- Agent 主动支架以内部 `scaffoldEvents` 记录类型、触发原因和对应回合，供报告诚实描述学习过程；它不是第四种用户可见状态，也不改变 25/0 计分；
- 提示分三级逐步增强：方向 → 案例或类比 → 接近答案的结构化线索；
- 第三次以后不继续泄露更多内容，提示用户可以继续尝试或点击答案；
- 用户最终答对时，根据 `hasRequestedHint` 决定 PASSED 或 PASSED_WITH_HINT；
- 两种状态都得 25 分。

### 代码与模型边界

模型负责：

- 理解用户语义；
- 判断回答分类；
- 提取正确证据、缺失点和误解；
- 判断 ADVANCING 或 STALLED；
- 生成反馈、追问、提示、完整答案和自然过渡。

代码负责：

- 固定四层顺序；
- 只允许一个主问题；
- 记录提示点击；
- 连续无进展计数；
- 三种完成状态；
- 25/0 分计算；
- 节点与任务完成；
- 对话和草稿持久化；
- 切换节点后的恢复；
- 拒绝非法模型状态转移。

### 计分与可见反馈

- 每个节点四层合计 100 分，每层固定 25 分；
- `PASSED` 与 `PASSED_WITH_HINT` 得 25 分，`PASSED_WITH_ANSWER` 得 0 分；
- 分数只是一种直观反馈和鼓励，不宣称是科学的能力测量、智力判断或严格掌握度；
- 材料整体只显示“已完成主题数 / 总主题数”和进度条，不计算跨主题平均分；
- 当前主题只显示主题名称、进行中或已完成状态，以及当前累计 0—100 分；
- 主诊断视图不再额外展示“学习进度”“掌握证据”等抽象指标，也不默认罗列四层名称；
- 节点完成后不弹出总结卡片、分层统计、徽章或额外完成度说明；节点列表和材料进度直接更新，聊天区只保留本轮自然反馈和继续导航；
- 三种层级状态继续作为计分依据和诊断报告证据保存。

### 闲聊与节点切换

- 闲聊不算作诊断尝试，不增加无进展计数；
- Agent 用一到两句自然回应支线，再用一句桥接语回到当前主问题；
- 用户明确要求稍后继续时可以停止追问；
- 每个节点保存独立消息、输入草稿、当前层、主问题和计数；
- 切换回来时只用普通语言轻量提示“接着刚才的问题”，随后恢复原问题，不暴露后台层级术语；
- 不重新生成问题，不丢失此前提示状态。

## 七、Agent 3 诊断报告

### 报告层级

采用任务级报告，并以知识节点作为内部章节。原因是用户上传的是一份材料，最终需要一份完整结论；节点级独立报告会造成碎片化。

### 生成时机

- 每完成一个节点，自动更新任务报告；
- 完成第一个节点后报告入口即可使用；
- 所有节点完成后生成最终版本并将任务标记为完成；
- 报告与任务一起保存在 IndexedDB。

### 固定结构

1. 材料名称与诊断时间；
2. 完成主题数与材料总体进度；
3. 节点结果总览；
4. 每个节点的四层状态与分数；
5. 已掌握内容；
6. 主要理解盲点；
7. 用户原话证据；
8. 本次引导和学会的内容；
9. 下一步学习建议；
10. 材料来源引用。

### 下载与分享

- 报告页面支持下载 Markdown；
- 提供适配打印的页面样式，允许打印或保存为 PDF；
- “分享”优先调用 Web Share API 分享报告文本或 Markdown 文件；
- 不支持时回退到复制报告摘要；
- 未产生报告时分享按钮禁用并说明原因；
- 不公开分享原始材料和完整聊天。

## 八、P1 交互与视觉决策

### 桌面布局

- 以下宽度是首轮实现起点，必须根据真实截图微调：工作区约 248px，可收起为约 64px；
- 节点列表约 240px；
- 对话区使用弹性宽度，建议不小于 520px；
- 诊断区约 304px，可收起；
- 小于 1200px 时默认收起工作区，诊断区改为抽屉；
- 小于 768px 进入移动端布局。

### 右侧诊断区的信息克制

- 材料整体：完成主题数、主题总数和一条清楚的进度条；
- 当前主题：主题名称、进行状态和 0—100 分；
- 不展示学习目标选择器、双重指标、默认展开的四层术语列表或节点完成总结卡；
- 更详细的四层证据放入诊断报告，不挤占实时对话界面。

### 移动端

- 默认全屏显示当前节点聊天；
- 左上角打开任务抽屉；
- 点击顶部节点名称打开节点抽屉；
- 右上角进度按钮打开诊断底部抽屉；
- 报告为独立全屏页面；
- 输入区保持在安全区域上方；
- 提示与答案位于输入框上方的紧凑操作条；
- 语音和发送位于输入框两侧。

### 任务菜单

- 重命名：对话框内编辑并立即保存；
- 置顶：置顶任务按最近更新时间排序；
- 分享：分享当前任务报告；
- 删除：确认后删除本地文件、解析文本、节点、消息和报告；
- 删除成功后提供短时撤销，不做批量删除；
- 搜索匹配任务标题和材料文件名。

### 语音输入

- 使用 Web Speech API 的 SpeechRecognition；
- 点击麦克风开始，再次点击停止；
- 显示实时或最终转写结果；
- 转写内容进入输入框，用户确认和编辑后再发送；
- 默认识别语言为 `zh-CN`；
- 功能检测失败、权限拒绝或识别错误时保留文字输入并给出明确提示；
- 不把语音文件发送给 DeepSeek，因为 DeepSeek V4 官方文本 API不提供语音转写能力。

### 视觉方向

主题概念候选：“温暖而有秩序的学习桌”。它用于帮助建立亲和、清晰的初始方向，不是不可调整的风格模板。

- 奶油、钴蓝、蜂蜜橙和深可可可作为第一轮色板候选，目标模式应根据真实页面效果保留、调整或替换；
- 柔和大圆角、粗线条描边、几何化图形、大量留白、孟菲斯式点线面装饰和轻微实体按压感；
- 学习进度反馈鲜明，但不复制多邻国的绿色、组件和角色动作；
- 标题或品牌文字使用有性格的中文字体，正文使用高可读中文无衬线字体；
- 图标采用统一圆角线性或双色风格；
- 不使用紫色渐变白底、玻璃拟态和通用 AI 仪表盘风格；
- 深色模式首版不做；
- 满足 WCAG AA 对比度、键盘操作和 `prefers-reduced-motion`。

### 卡皮巴拉“维塔”

- 定位：贯穿产品的唯一学习伙伴与品牌吉祥物；
- 性格：沉稳、好奇、耐心、不评判、偶尔有轻松幽默；
- 不为每个节点设计不同家教；
- 节点用彩色书脊、概念符号和状态点区分；
- 维塔可以作为 Agent 消息头像，但始终是同一个角色；
- 为维塔设计一个简洁、原创、可识别的学习道具；具体配色和道具由最终整体视觉决定，不受早期候选方案绑定；
- 首版资产优先保持精简：默认、等待上传、处理中、给提示、真诚鼓励、展示答案、错误共七种可复用状态；
- 动效以轻微呼吸、眨眼、点头和纸屑庆祝为主，避免高频干扰。

## 九、分阶段实施顺序

### 阶段 0：锁定开发上下文

- 将已确认设计收敛为 `DESIGN_GUIDE.md`；
- 创建短小的 `AGENTS.md`；
- 将本计划转为最终版；
- 定义首条端到端验收材料与预期；
- 初始化版本控制。

验收：新窗口只读三份核心文档即可准确复述产品、边界和执行顺序。

### 阶段 1：工程脚手架与质量门

- 创建 Next.js TypeScript 项目；
- 配置 Tailwind、ESLint、格式化、Vitest、RTL、Playwright；
- 配置环境变量校验、错误结构和安全头；
- 建立 `PROJECT_MAP.md`；
- 确保开发、构建、类型检查和空测试全部通过。

检查点：工程为空但稳定，所有质量命令成功。

### 阶段 2：领域模型与状态机

- 定义 Task、Material、Node、Stage、Message、Report 类型；
- 实现唯一 reducer、事件和计分；
- 实现提示三级、连续无进展和自动答案规则；
- 编写覆盖所有合法与非法转移的单元测试。

检查点：不接模型也能用测试证明四层流程不会漂移。

### 阶段 3：本地数据与主界面骨架

- 建立 IndexedDB schema 和迁移；
- 实现任务恢复、搜索、重命名、置顶、删除与撤销；
- 搭建桌面三大区域和移动端导航骨架；
- 实现无材料时的禁用输入区和上传入口。

检查点：刷新页面后本地任务与 UI 状态正确恢复。

### 阶段 4：品牌基础与维塔资产

- 锁定颜色、字体、间距、圆角、阴影与组件令牌；
- 生成原创维塔角色设定与七种精简状态资产；
- 建立图标、按钮、卡片、气泡、进度与状态标签基础组件；
- 验证桌面和手机的视觉一致性。

检查点：空主界面已经具有最终品牌方向，不使用临时通用模板。

### 阶段 5：文本材料纵向闭环

- 先实现 Markdown/TXT 读取；
- 创建任务、真实读取进度和错误状态；
- 建立 DeepSeek 服务端代理、重试和 Zod 校验；
- 实现 Agent 1 分块提取、完整知识地图、主题聚合与覆盖审计；
- 自动进入第一个节点并生成记忆层问题。

检查点：真实 TXT/Markdown 可以从上传一路走到第一个真实问题。

### 阶段 6：全部文件格式

- 实现 DOCX、PPTX、文本 PDF；
- 实现图片 OCR 与扫描 PDF 回退；
- 加入文件类型、大小、页数、文本长度和 OCR 错误处理；
- 为每种格式建立测试夹具；
- 验证解析来源位置。

检查点：每种承诺格式至少有一个真实样本成功生成节点。

### 阶段 7：Agent 2 完整诊断教学

- 实现层级问题契约；
- 接入回答分类、掌握证据、误解和进展判断；
- 实现针对性反馈、追问、自动支架和三轮无进展规则；
- 实现多级提示和答案；
- 实现节点切换、输入草稿、闲聊恢复和历史上下文；
- 接入右侧实时诊断与分数。

检查点：一个真实节点可以走完四层，并覆盖通过、提示后通过和给答案通过。

### 阶段 8：完整桌面工作区

- 完成工作区、节点列表、聊天区和诊断区的精细交互；
- 实现收起、搜索、任务菜单、节点状态和滚动行为；
- 实现发送中、停止、重试和错误消息；
- 完成加载、空状态、失败和节点完成后的无冗余状态更新，不增加完成总结卡片。

检查点：1440px 桌面主流程视觉与交互完整。

### 阶段 9：诊断报告、下载与分享

- 实现 Agent 3 自动更新的任务级报告；
- 实现报告查看、Markdown 下载和打印 PDF；
- 实现 Web Share API 与复制回退；
- 验证用户原话、状态和材料引用忠实。

检查点：完成至少一个节点后可以获得、下载并分享真实报告。

### 阶段 10：语音输入

- 接入 SpeechRecognition 与权限处理；
- 实现录音状态、转写、编辑后发送；
- 处理不支持、无权限、无声音、识别失败和中断；
- 验证桌面与移动端支持范围。

检查点：支持的真实浏览器中可以通过语音完成一次回答。

### 阶段 11：移动端完整适配

- 完成任务抽屉、节点抽屉、诊断底部抽屉和报告页面；
- 处理软键盘、安全区、长消息、长节点名和横竖屏；
- 确认所有按钮可触达且不发生横向溢出；
- 验证触控和返回行为。

检查点：390×844 视口可以完成上传到报告的完整流程。

### 阶段 12：安全、可靠性与性能

- 安全不是到本阶段才补做；阶段 1 起所有输入、文件、模型和存储边界都必须按本节约束实现，本阶段负责集中审计；
- 对扩展名、MIME、文件签名、解压后体积、条目数、压缩比、页数、像素和文本长度做组合校验，防范伪造类型、畸形文件、ZIP/XML 炸弹与资源耗尽；
- Office、PDF 和图片只提取受限纯文本或像素，不执行宏、脚本、外部链接或文档内指令；
- 将材料与用户输入视为不可信数据，与系统指令严格分隔；模型无工具权限，不能执行代码、访问环境变量、读取其他任务或发起任意网络请求；
- API 代理只允许固定模型、固定上游地址和经过 Zod 校验的参数，并实施请求大小、并发、频率、超时、输出 token 和重试上限；
- React 只渲染转义文本；Markdown 使用严格白名单清理，禁止原始 HTML 与 `dangerouslySetInnerHTML`；
- 配置 CSP 等安全响应头、同源策略和错误脱敏，不向浏览器、日志或报告暴露密钥、系统提示词、堆栈和完整敏感材料；
- 对模型结构化输出进行 schema 校验，所有状态变化仍由代码授权；
- 审核 IndexedDB 配额与失败恢复；
- 优化 OCR worker、长列表、消息渲染和包体；
- 加入恶意提示注入、XSS、伪造文件、解压炸弹、超长输入、越权状态转移和模型异常输出测试；
- 检查依赖漏洞、秘密泄漏和生产安全头，不接受可达的高危或严重漏洞。

检查点：核心错误路径可恢复，无密钥和敏感材料泄漏。

### 阶段 13：全面自动化与浏览器验收

- 运行全部单元、组件、集成和端到端测试；
- 用真实材料覆盖所有格式；
- 验证刷新恢复、节点切换、三种状态、报告、语音和分享；
- 检查桌面、平板、手机视觉；
- 检查控制台错误、网络失败、可访问性和性能；
- 修复根因，不堆叠临时补丁。

检查点：构建、类型、测试和真实浏览器验收全部通过。

### 阶段 14：文档、本地交付与最终审计

- 更新 `PROJECT_MAP.md` 与真实代码一致；
- 编写面向用户和面试官的 `README.md`；
- 确认 `DESIGN_GUIDE.md` 与实际行为一致；
- 编写本地环境变量、启动、测试和生产构建说明；
- 在本地生产构建上运行桌面与手机端冒烟测试；
- 审核未使用代码、依赖、配置和 TODO；
- 输出最终功能清单、测试结果、本地访问方式和已知限制。

最终检查点：本地生产构建可以在桌面和手机视口完成完整真实流程，文档与代码一致；公网部署明确留到后续任务。

## 十、目标模式运行纪律

- 每个阶段开始前读取相关设计章节与 `PROJECT_MAP.md`；
- 每次只实施一个可验证任务；
- 每个阶段结束必须构建、测试并检查真实 diff；
- 失败时先定位根因，不通过增加无关抽象绕过；
- 不因工作量大而擅自删除功能；
- 不因未来可能使用而增加预留；
- 发现规格冲突立即停止该分支并回到权威文档；
- 结构变化同步更新 `PROJECT_MAP.md`；
- 设计变化先更新 `DESIGN_GUIDE.md` 再改代码；
- benchmark 不阻塞首版，但确定性状态机测试必须同步完成；
- 目标只在全部最终检查点完成后标记完成。

## 十一、已锁定决策

- 本轮只要求本地完整运行和真实验收，不执行公网部署；
- 历史只在同一浏览器与设备恢复，分享只分享报告，不创建公开任务链接；
- 一项任务只上传一个文件，采用本章确定的文件限制；
- Office 只支持现代 `.docx` 与 `.pptx`，不支持旧版 `.doc` 与 `.ppt`；
- 三个 Agent 统一使用 `deepseek-v4-flash`；交互路径关闭思考，Agent 1 覆盖审计和 Agent 3 报告使用低强度思考；
- Agent 1 采用完整知识地图、相关知识聚合为诊断主题、一次建议完成一个主题、明示未诊断内容；
- 学习目标由 Agent 根据材料在内部建立；没有确认弹窗、目标卡片和固定用途选项，用户只需在对话中自然表达调整；
- 材料整体显示完成主题数和进度条，当前主题显示 0—100 分；四层是后台状态机，不在主诊断区罗列；
- 节点完成不弹出总结卡、徽章或分层统计，材料进度、节点状态和当前分数直接更新；
- `.env.local` 已由用户提供本地专用开发 Key，目标模式必须执行少量真实模型冒烟测试且不得读取、输出、记录或提交密钥值；
- 语音采用浏览器 SpeechRecognition，不额外接入第二家语音服务；
- Agent 主动支架内部留痕，但不增加第四种用户可见状态；
- 安全是从工程第一阶段开始的基础约束，而不是部署前补丁。

以下 P1 决策由 Codex 根据授权确定，不再作为阻塞问题：

- 语音采用浏览器 SpeechRecognition，在不支持时明确提示并保留文字输入；
- 报告采用任务级自动报告、Markdown 下载和打印为 PDF；
- 正式采用单一卡皮巴拉“维塔”；整体偏好多邻国式的亲和、活泼和完成反馈，但圆角、描边、孟菲斯等仅是参考原则，目标模式必须根据真实页面效果自主设计和迭代，不机械套用风格清单。
